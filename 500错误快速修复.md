# 500 错误快速修复

## 🚨 立即修复

运行修复脚本：

```bash
cd ~/gksys/GkSystem
chmod +x fix-500-now.sh
sudo ./fix-500-now.sh
```

## 🔍 手动排查步骤

如果脚本仍然失败，按以下步骤排查：

### 步骤1：查看错误日志（最重要！）

```bash
# 查看错误日志
sudo tail -30 /var/log/nginx/error.log

# 或查看 canteen 专用日志
sudo tail -30 /var/log/nginx/canteen-error.log
```

**请提供错误日志的具体内容，这是诊断的关键！**

### 步骤2：检查前端文件

```bash
# 检查文件是否存在
ls -la ~/gksys/GkSystem/frontend/dist/index.html

# 如果不存在，重新构建
cd ~/gksys/GkSystem/frontend
npm run build
```

### 步骤3：检查 Nginx 配置路径

```bash
# 查看配置
sudo cat /etc/nginx/sites-available/canteen | grep root

# 获取实际路径
realpath ~/gksys/GkSystem/frontend/dist
```

### 步骤4：手动修复配置

```bash
# 获取实际路径
REAL_PATH=$(realpath ~/gksys/GkSystem/frontend/dist)
echo "实际路径: $REAL_PATH"

# 备份配置
sudo cp /etc/nginx/sites-available/canteen /etc/nginx/sites-available/canteen.bak

# 创建新配置（使用绝对路径）
sudo tee /etc/nginx/sites-available/canteen > /dev/null <<EOF
server {
    listen 80;
    server_name _;

    access_log /var/log/nginx/canteen-access.log;
    error_log /var/log/nginx/canteen-error.log;

    location / {
        root $REAL_PATH;
        index index.html;
        try_files \$uri \$uri/ /index.html;
    }

    location /api {
        proxy_pass http://127.0.0.1:5000/api;
        proxy_set_header Host \$host;
        proxy_set_header X-Real-IP \$remote_addr;
        proxy_set_header X-Forwarded-For \$proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto \$scheme;
    }

    location /health {
        proxy_pass http://127.0.0.1:5000/health;
        proxy_set_header Host \$host;
    }
}
EOF

# 测试配置
sudo nginx -t

# 设置权限
sudo chmod -R 755 "$REAL_PATH"
sudo chown -R www-data:www-data "$REAL_PATH"

# 重启 Nginx
sudo systemctl restart nginx

# 测试
curl http://localhost/
```

## 📋 常见错误和解决方案

### 错误1：No such file or directory

**原因：** 路径不存在或配置路径错误

**解决：**
```bash
# 检查路径
ls -la ~/gksys/GkSystem/frontend/dist/

# 更新配置中的路径
sudo sed -i "s|root.*|root $(realpath ~/gksys/GkSystem/frontend/dist);|g" /etc/nginx/sites-available/canteen
sudo nginx -t
sudo systemctl restart nginx
```

### 错误2：Permission denied

**原因：** Nginx 用户无法读取文件

**解决：**
```bash
# 设置权限
sudo chmod -R 755 ~/gksys/GkSystem/frontend/dist
sudo chown -R www-data:www-data ~/gksys/GkSystem/frontend/dist
sudo systemctl restart nginx
```

### 错误3：Directory index forbidden

**原因：** index.html 不存在或权限问题

**解决：**
```bash
# 检查文件
ls -la ~/gksys/GkSystem/frontend/dist/index.html

# 如果不存在，重新构建
cd ~/gksys/GkSystem/frontend
npm run build
```

## 🔧 实时调试

```bash
# 终端1：查看错误日志
sudo tail -f /var/log/nginx/error.log

# 终端2：访问测试
curl http://localhost/

# 观察终端1的输出
```

## 📚 相关脚本

- `fix-500-now.sh` - 自动修复脚本
- `diagnose-500-error.sh` - 诊断脚本
- `check-nginx-config.sh` - 配置检查脚本

---

**先运行修复脚本，如果失败，请提供错误日志的具体内容！**

