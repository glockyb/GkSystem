# 项目问题解答文档

本文档回答关于项目的常见问题。

## 📋 目录

1. [训练数据是否写死的？](#训练数据是否写死的)
2. [完成工程的思路是什么？](#完成工程的思路是什么)
3. [前后端如何结合和联系？](#前后端如何结合和联系)
4. [训练模型在哪一层？](#训练模型在哪一层)
5. [训练模型如何进行训练和训练数据的提供？](#训练模型如何进行训练和训练数据的提供)
6. [如果后期想添加训练数据，在哪里加？](#如果后期想添加训练数据在哪里加)

---

## ❓ 训练数据是否写死的？

### 答案：**不是写死的！**

训练数据全部存储在 **MySQL 数据库** 中，训练时会动态从数据库加载。

### 数据来源

1. **菜品数据**：`dishes` 表
   - 通过前端界面添加
   - 通过SQL直接插入
   - 通过Python脚本导入

2. **评分数据**：`ratings` 和 `consumption_records` 表
   - 用户在前端评分自动保存
   - 通过SQL批量导入
   - 通过API接口添加

3. **特征数据**：`dish_features` 表
   - 由数据预处理自动生成
   - 从菜品和评分数据计算得出

### 数据流程

```
用户操作 → 数据库（MySQL） → 数据预处理 → 特征提取 → 模型训练 → 推荐服务
```

**详细说明**：查看 [训练数据说明.md](./训练数据说明.md)

---

## 🎯 完成工程的思路是什么？

### 1. 需求分析

- **核心需求**：个性化菜品推荐
- **次要需求**：用户管理、菜品浏览、评分系统

### 2. 技术选型

- **前端**：Vue 3（现代化、响应式）
- **后端**：Flask（轻量级、灵活）
- **数据库**：MySQL（结构化数据）+ Redis（缓存）
- **算法**：混合推荐（协同过滤 + 内容推荐）

### 3. 架构设计

采用 **前后端分离** + **微服务化** 的设计：

```
前端（Vue） ←→ 后端API（Flask） ←→ 数据库（MySQL/Redis）
                              ↓
                        推荐算法模块
                              ↓
                        模型训练（离线）
```

### 4. 开发流程

1. **数据库设计**：设计表结构（用户、菜品、评分等）
2. **后端开发**：API接口、推荐算法、数据处理
3. **前端开发**：UI界面、用户交互、API调用
4. **模型训练**：数据预处理、特征工程、模型训练
5. **系统集成**：前后端联调、API对接
6. **部署上线**：Docker容器化、一键部署

### 5. 设计原则

- ✅ **模块化**：功能模块独立，易于维护
- ✅ **可扩展**：易于添加新功能
- ✅ **可部署**：Docker容器化，环境一致
- ✅ **数据驱动**：数据来自数据库，易于更新

**详细说明**：查看 [系统架构说明.md](./系统架构说明.md)

---

## 🔗 前后端如何结合和联系？

### 连接方式

#### 1. 开发环境

```
前端 (Vite Dev Server: 8080)
  ↓ HTTP请求 (/api/*)
  ↓ Vite Proxy 代理
后端 (Flask: 5000)
```

**配置**：`frontend/vite.config.js`
```javascript
proxy: {
  '/api': {
    target: 'http://localhost:5000',
    changeOrigin: true
  }
}
```

#### 2. 生产环境（Docker）

```
前端 (Nginx: 80)
  ↓ HTTP请求 (/api/*)
  ↓ Nginx 反向代理
后端 (Flask: 5000)
```

**配置**：`frontend/nginx.conf`
```nginx
location /api {
    proxy_pass http://backend:5000/api;
}
```

### API通信

- **统一前缀**：`/api/v1`
- **数据格式**：JSON
- **认证方式**：JWT Token

### 通信流程

```
1. 前端发起请求
   api.dishes.getList()
   
2. Axios 自动添加 Token
   Authorization: Bearer {token}
   
3. Vite/Nginx 代理到后端
   POST http://localhost:5000/api/v1/dishes
   
4. 后端处理请求
   @bp.route('/dishes', methods=['GET'])
   
5. 返回 JSON 响应
   { dishes: [...], total: 100 }
   
6. 前端接收并渲染
   dishes.value = response.dishes
```

**详细说明**：查看 [前后端连接说明.md](./前后端连接说明.md)

---

## 🤖 训练模型在哪一层？

### 模型位置

训练模型在 **后端业务逻辑层**，具体位置：

```
backend/
├── models/
│   └── recommender.py      # 推荐算法实现（模型层）
│       ├── CollaborativeFiltering    # 协同过滤
│       ├── ContentBasedRecommender   # 内容推荐
│       └── HybridRecommender         # 混合推荐
│
├── data/
│   └── preprocessor.py     # 数据预处理（数据层）
│
└── train_model.py          # 训练入口脚本
```

### 分层架构

```
┌─────────────────────────┐
│   前端层 (Vue)           │  ← 用户界面
└────────┬────────────────┘
         │ HTTP API
         ▼
┌─────────────────────────┐
│   API层 (Flask Routes)   │  ← 接口层
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│  业务逻辑层              │
│  ┌──────────────────┐   │
│  │ 推荐算法模块     │   │  ← 模型层（这里）
│  │  (Recommender)   │   │
│  └──────────────────┘   │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│  数据层                  │
│  ┌──────────┐ ┌────────┐│
│  │ MySQL    │ │ Redis  ││
│  └──────────┘ └────────┘│
└─────────────────────────┘
```

### 模型训练（离线）

训练过程：

```
数据预处理 (data/preprocessor.py)
  ↓
特征工程
  ↓
模型训练 (models/recommender.py)
  ↓
保存模型文件 (.pkl)
```

**详细说明**：查看 [系统架构说明.md](./系统架构说明.md#训练模型位置和流程)

---

## 📊 训练模型如何进行训练和训练数据的提供？

### 训练流程

#### 第一步：数据预处理

**脚本**：`backend/data/preprocessor.py`

**流程**：
```python
1. 从MySQL加载数据
   - dishes (菜品)
   - ratings (评分)
   - consumption_records (消费记录)

2. 数据清洗
   - 处理缺失值
   - 剔除异常值

3. 特征工程
   - 类别编码
   - 价格区间
   - 平均评分
   - TF-IDF向量化（文本特征）
   - PCA降维（32维）

4. 保存特征到数据库
   - 保存到 dish_features 表
```

#### 第二步：模型训练

**脚本**：`backend/train_model.py`

**流程**：
```python
1. 数据预处理
   preprocessor.process_all()

2. 训练协同过滤模型
   - 加载评分数据 (user_id, dish_id, rating)
   - 使用 SVD 算法（矩阵分解）
   - 学习用户和菜品的潜在因子
   - 保存到 cf_model.pkl

3. 训练内容推荐模型
   - 加载菜品特征（从 dish_features 表）
   - 使用 KNN 算法（余弦相似度）
   - 建立菜品相似度索引
   - 保存到 cb_model.pkl
```

### 训练数据提供

#### 1. 数据来源（MySQL数据库）

- **`dishes` 表**：菜品基本信息
  ```sql
  INSERT INTO dishes (name, category, price, description) VALUES
  ('红烧肉', '荤菜', 12.00, '经典红烧肉');
  ```

- **`ratings` 表**：用户评分
  ```sql
  INSERT INTO ratings (user_id, dish_id, rating) VALUES
  (1, 1, 5);
  ```

- **`consumption_records` 表**：消费记录（可选）
  ```sql
  INSERT INTO consumption_records (user_id, dish_id, rating) VALUES
  (1, 1, 5);
  ```

#### 2. 数据加载（训练时）

```python
# backend/models/recommender.py

def load_ratings(self):
    """从数据库加载评分数据"""
    query = """
        SELECT user_id, dish_id, rating
        FROM ratings
        WHERE rating IS NOT NULL
        UNION
        SELECT user_id, dish_id, rating
        FROM consumption_records
        WHERE rating IS NOT NULL
    """
    df = pd.read_sql(query, connection)  # 从MySQL加载
    return df
```

### 如何运行训练

```bash
# 方式1：直接运行训练脚本
cd backend
python train_model.py

# 方式2：Docker环境
docker-compose exec backend python train_model.py

# 方式3：使用启动脚本（自动检测并训练）
./启动后端.sh
```

**详细说明**：
- [系统架构说明.md](./系统架构说明.md#训练模型位置和流程)
- [训练数据说明.md](./训练数据说明.md)

---

## ➕ 如果后期想添加训练数据，在哪里加？

### 方式一：通过前端界面（最简单）

1. **注册/登录账号**
2. **浏览菜品并评分**
   - 评分数据会自动保存到 `ratings` 表
3. **重新训练模型**
   ```bash
   python train_model.py
   ```

### 方式二：通过SQL直接插入

#### 1. 添加菜品数据

```sql
-- 连接到数据库
mysql -u root -p canteen_recommendation

-- 添加菜品
INSERT INTO dishes (name, category, price, description, image_url) VALUES
('新菜品', '荤菜', 15.00, '菜品描述', '/images/new-dish.jpg');
```

#### 2. 添加评分数据

```sql
-- 添加评分（需要先有用户和菜品）
INSERT INTO ratings (user_id, dish_id, rating, comment) VALUES
(1, 10, 5, '很好吃！');
```

#### 3. 批量导入

```sql
-- 批量添加菜品
INSERT INTO dishes (name, category, price, description) VALUES
('菜品1', '荤菜', 12.00, '描述1'),
('菜品2', '素菜', 8.00, '描述2'),
('菜品3', '荤菜', 15.00, '描述3');

-- 批量添加评分
INSERT INTO ratings (user_id, dish_id, rating) VALUES
(1, 1, 5),
(1, 2, 4),
(1, 3, 5);
```

### 方式三：通过Python脚本

创建脚本 `backend/scripts/import_data.py`：

```python
from utils.database import db

connection = db.get_connection()
cursor = connection.cursor()

# 添加菜品
cursor.execute("""
    INSERT INTO dishes (name, category, price, description)
    VALUES (%s, %s, %s, %s)
""", ('新菜品', '荤菜', 15.00, '描述'))

# 添加评分
cursor.execute("""
    INSERT INTO ratings (user_id, dish_id, rating)
    VALUES (%s, %s, %s)
""", (1, 1, 5))

connection.commit()
```

运行：
```bash
cd backend
python scripts/import_data.py
```

### 方式四：通过API接口

```bash
# 使用cURL添加评分
curl -X POST http://localhost:5000/api/v1/ratings \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "dish_id": 1,
    "rating": 5,
    "comment": "很好吃"
  }'
```

### 添加数据后的操作

#### 1. 重新进行数据预处理

```bash
cd backend
python data/preprocessor.py
```

这会：
- 重新加载所有菜品和评分数据
- 重新计算特征
- 更新 `dish_features` 表

#### 2. 重新训练模型

```bash
python train_model.py
```

这会：
- 使用新的数据训练模型
- 更新模型文件（`.pkl`）

**详细说明**：查看 [训练数据说明.md](./训练数据说明.md)

---

## 📚 相关文档

- [系统架构说明.md](./系统架构说明.md) - 系统架构和工程思路
- [前后端连接说明.md](./前后端连接说明.md) - 前后端连接方式
- [训练数据说明.md](./训练数据说明.md) - 如何添加训练数据
- [PPT演示文档.md](./PPT演示文档.md) - 项目演示PPT

---

## 💡 总结

### 核心要点

1. **训练数据不是写死的**：来自MySQL数据库，可动态添加
2. **前后端分离**：通过RESTful API连接
3. **模型训练在离线层**：`backend/models/` 和 `backend/data/`
4. **数据来源**：MySQL数据库的 `dishes`、`ratings`、`consumption_records` 表
5. **添加数据位置**：直接在MySQL数据库表中添加，然后重新训练模型

### 数据流程总结

```
添加数据 → MySQL数据库 → 数据预处理 → 特征提取 → 模型训练 → 推荐服务
```

---

**如有其他问题，请查看相关文档或联系项目维护者！**

