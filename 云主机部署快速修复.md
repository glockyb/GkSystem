# 云主机部署快速修复指南

## 🚨 您遇到的问题

```
Error: failed to resolve reference "docker.io/library/mysql:8.0"
dial tcp: lookup docker.mirrors.ustc.edu.cn on 127.0.0.53:53: no such host
所有镜像仓库都不可用，请检查网络连接
```

**问题原因：** DNS 解析失败 + 网络连接问题

## ✅ 立即解决方案

### 第一步：运行网络修复脚本（最重要）

```bash
# 在云主机上运行
cd /path/to/GkSystem
sudo ./fix-network-dns.sh
```

这个脚本会：
- ✅ 检查网络连接
- ✅ 自动修复 DNS 配置（使用公共 DNS：8.8.8.8, 114.114.114.114）
- ✅ 测试镜像仓库连接
- ✅ 配置 Docker 镜像加速器
- ✅ 配置 Docker 代理（如果需要）

### 第二步：检查云主机安全组

**关键：** 确保云主机安全组允许出站流量！

#### 阿里云/腾讯云：

1. 登录云服务器控制台
2. 找到 **安全组** 配置
3. 检查 **出站规则**：
   - ✅ 允许 HTTPS（443端口）出站
   - ✅ 允许 DNS（53端口 UDP/TCP）出站
   - ✅ 允许 HTTP（80端口）出站（可选）

**配置示例：**
```
出站规则：
- 协议：TCP，端口：443，目标：0.0.0.0/0，操作：允许
- 协议：UDP，端口：53，目标：0.0.0.0/0，操作：允许
```

### 第三步：手动修复 DNS（如果脚本失败）

```bash
# 备份原配置
sudo cp /etc/resolv.conf /etc/resolv.conf.bak

# 配置公共 DNS
sudo tee /etc/resolv.conf > /dev/null <<EOF
nameserver 8.8.8.8
nameserver 8.8.4.4
nameserver 114.114.114.114
nameserver 223.5.5.5
EOF

# 如果使用 systemd-resolved
sudo systemctl restart systemd-resolved

# 测试 DNS
nslookup docker.io
nslookup docker.mirrors.ustc.edu.cn
```

### 第四步：验证网络连接

```bash
# 测试基本网络
ping -c 3 8.8.8.8

# 测试 DNS
nslookup baidu.com
nslookup docker.io

# 测试 HTTPS
curl -I https://www.baidu.com
curl -I https://docker.io
```

### 第五步：重新部署

```bash
# 如果网络修复成功，重新运行部署
./deploy-remote.sh
```

---

## 🔍 详细排查步骤

### 1. 检查云主机网络类型

```bash
# 查看网络接口
ip addr show

# 查看路由
ip route

# 测试外网连接
curl -I https://www.baidu.com
```

### 2. 检查 DNS 配置

```bash
# 查看当前 DNS
cat /etc/resolv.conf

# 测试 DNS 解析
nslookup docker.io
dig docker.io
host docker.io
```

### 3. 检查防火墙

```bash
# 检查 UFW
sudo ufw status

# 检查 firewalld
sudo firewall-cmd --list-all

# 确保允许出站流量（UFW 默认允许，但检查一下）
```

### 4. 检查是否需要代理

```bash
# 检查代理环境变量
env | grep -i proxy

# 如果云主机需要通过代理访问外网
export http_proxy=http://proxy-server:port
export https_proxy=http://proxy-server:port
```

---

## 🎯 云主机安全组配置（重要！）

### 阿里云安全组

1. **登录阿里云控制台**
2. **ECS → 安全组 → 配置规则**
3. **添加入站规则：**
   - SSH (22)
   - 自定义TCP (8080)
   - 自定义TCP (5000) - 可选
4. **添加出站规则（重要！）：**
   - 自定义TCP (443) - HTTPS
   - 自定义UDP (53) - DNS
   - 自定义TCP (80) - HTTP（可选）

### 腾讯云安全组

1. **登录腾讯云控制台**
2. **云服务器 → 安全组 → 规则配置**
3. **配置入站规则：**
   - SSH (22)
   - 自定义 (8080)
   - 自定义 (5000) - 可选
4. **配置出站规则（重要！）：**
   - HTTPS (443)
   - DNS (53 UDP)
   - HTTP (80) - 可选

---

## 💡 常见云主机网络问题

### 问题1：内网 IP + 外网 IP 配置

**症状：** 云主机有内网 IP，通过外网 IP 访问

**解决：**
1. 确保外网 IP 已绑定
2. 确保路由配置正确
3. 检查安全组规则

### 问题2：NAT 网关问题

**症状：** 云主机通过 NAT 网关访问外网

**解决：**
1. 检查 NAT 网关路由表
2. 确保 NAT 网关允许所需流量
3. 检查 NAT 网关带宽限制

### 问题3：VPC 网络配置

**症状：** 云主机在 VPC 中，无法访问外网

**解决：**
1. 检查 VPC 路由表
2. 确保有到 Internet 网关的路由
3. 检查网络 ACL 规则

---

## 🆘 如果所有方法都失败

### 备用方案：离线部署

如果网络问题无法解决，可以使用离线镜像：

#### 1. 在本地或其他网络正常的服务器上

```bash
# 拉取所有需要的镜像
docker pull mysql:8.0
docker pull redis:7-alpine
docker pull nginx:alpine
docker pull python:3.9-slim
docker pull node:18-alpine

# 打包镜像
docker save mysql:8.0 | gzip > mysql-8.0.tar.gz
docker save redis:7-alpine | gzip > redis-7-alpine.tar.gz
docker save nginx:alpine | gzip > nginx-alpine.tar.gz
docker save python:3.9-slim | gzip > python-3.9-slim.tar.gz
docker save node:18-alpine | gzip > node-18-alpine.tar.gz
```

#### 2. 上传到云主机

```bash
# 在本地执行
scp *.tar.gz username@your-server-ip:~/
```

#### 3. 在云主机上加载

```bash
# 在云主机上执行
cd ~
gunzip -c mysql-8.0.tar.gz | docker load
gunzip -c redis-7-alpine.tar.gz | docker load
gunzip -c nginx-alpine.tar.gz | docker load
gunzip -c python-3.9-slim.tar.gz | docker load
gunzip -c node-18-alpine.tar.gz | docker load

# 验证镜像
docker images
```

#### 4. 运行部署

```bash
cd /path/to/GkSystem
./deploy-remote.sh
```

---

## 📋 检查清单

部署前：
- [ ] 云主机安全组允许 HTTPS (443) 出站
- [ ] 云主机安全组允许 DNS (53) 出站
- [ ] DNS 配置正确（运行 fix-network-dns.sh）
- [ ] 网络连接正常（ping 8.8.8.8）
- [ ] HTTPS 连接正常（curl https://www.baidu.com）
- [ ] DNS 解析正常（nslookup docker.io）

部署时：
- [ ] 先运行网络修复脚本
- [ ] 再运行部署脚本
- [ ] 使用 screen 保持会话（防止中断）

---

## 📚 相关文档

- [fix-network-dns.sh](./fix-network-dns.sh) - 网络 DNS 修复脚本
- [云主机网络问题排查.md](./云主机网络问题排查.md) - 详细排查文档
- [fix-deploy-issue.sh](./fix-deploy-issue.sh) - 部署问题修复脚本
- [部署故障处理指南.md](./部署故障处理指南.md) - 部署故障处理

---

## ✅ 快速修复命令（复制粘贴）

```bash
# 1. 修复 DNS 配置
sudo tee /etc/resolv.conf > /dev/null <<EOF
nameserver 8.8.8.8
nameserver 8.8.4.4
nameserver 114.114.114.114
nameserver 223.5.5.5
EOF

# 2. 重启 DNS 服务
sudo systemctl restart systemd-resolved 2>/dev/null || true

# 3. 测试 DNS
nslookup docker.io

# 4. 如果 DNS 正常，运行网络修复脚本
sudo ./fix-network-dns.sh

# 5. 重新部署
./deploy-remote.sh
```

---

**关键提示：先修复 DNS 和网络，再检查云主机安全组，最后重新部署！**

