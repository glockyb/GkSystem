# 训练数据说明文档

## 📋 目录
1. [训练数据来源](#训练数据来源)
2. [如何添加训练数据](#如何添加训练数据)
3. [数据结构说明](#数据结构说明)
4. [数据导入示例](#数据导入示例)

---

## 🔍 训练数据来源

### 重要说明

**训练数据不是写死的！** 所有数据都存储在 **MySQL 数据库**中，训练时会从数据库动态加载。

### 数据来源表

训练数据主要来自以下数据库表：

1. **`dishes` 表** - 菜品基本信息
2. **`ratings` 表** - 用户评分数据
3. **`consumption_records` 表** - 用户消费记录
4. **`dish_features` 表** - 菜品特征数据（由数据预处理生成）

---

## ➕ 如何添加训练数据

### 方式一：通过前端界面添加（推荐）

1. **注册/登录账号**
2. **浏览菜品并评分**：评分数据会自动保存到 `ratings` 表
3. **重新训练模型**：评分后可以手动触发模型重新训练

### 方式二：通过SQL直接插入

#### 1. 添加菜品数据

```sql
-- 连接到数据库
mysql -u root -p canteen_recommendation

-- 添加菜品
INSERT INTO dishes (name, category, price, description, image_url) VALUES
('宫保鸡丁', '荤菜', 15.00, '经典川菜宫保鸡丁，麻辣鲜香', '/images/gongbaojiding.jpg'),
('麻婆豆腐', '素菜', 8.00, '川味麻婆豆腐，麻辣鲜香', '/images/mapodoufu.jpg'),
('糖醋里脊', '荤菜', 14.00, '酸甜可口的糖醋里脊', '/images/tangculiji.jpg');
```

#### 2. 添加评分数据

```sql
-- 添加评分（需要先有用户和菜品）
INSERT INTO ratings (user_id, dish_id, rating, comment) VALUES
(1, 1, 5, '很好吃！'),
(1, 2, 4, '还不错'),
(1, 3, 5, '非常推荐');

-- 或者添加消费记录（带评分）
INSERT INTO consumption_records (user_id, dish_id, consumption_time, price, rating) VALUES
(1, 1, NOW(), 15.00, 5),
(1, 2, NOW(), 8.00, 4),
(1, 3, NOW(), 14.00, 5);
```

#### 3. 批量导入数据

**准备CSV文件**（例如：`dishes.csv`）：
```csv
name,category,price,description
红烧肉,荤菜,12.00,经典红烧肉，肥而不腻
西红柿鸡蛋,素菜,10.00,家常西红柿炒鸡蛋
```

**使用SQL导入**：
```sql
LOAD DATA LOCAL INFILE '/path/to/dishes.csv'
INTO TABLE dishes
FIELDS TERMINATED BY ','
ENCLOSED BY '"'
LINES TERMINATED BY '\n'
IGNORE 1 ROWS
(name, category, price, description);
```

### 方式三：通过Python脚本导入

创建脚本 `backend/scripts/import_data.py`：

```python
import sys
import os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from utils.database import db

# 连接数据库
connection = db.get_connection()
cursor = connection.cursor()

# 添加菜品
dishes = [
    ('宫保鸡丁', '荤菜', 15.00, '经典川菜宫保鸡丁', '/images/gongbaojiding.jpg'),
    ('麻婆豆腐', '素菜', 8.00, '川味麻婆豆腐', '/images/mapodoufu.jpg'),
    ('糖醋里脊', '荤菜', 14.00, '酸甜可口的糖醋里脊', '/images/tangculiji.jpg'),
]

for dish in dishes:
    cursor.execute("""
        INSERT INTO dishes (name, category, price, description, image_url)
        VALUES (%s, %s, %s, %s, %s)
    """, dish)

connection.commit()
print(f"成功导入 {len(dishes)} 条菜品数据")

# 添加评分（示例）
ratings = [
    (1, 1, 5, '很好吃！'),
    (1, 2, 4, '还不错'),
    (1, 3, 5, '非常推荐'),
]

for rating in ratings:
    cursor.execute("""
        INSERT INTO ratings (user_id, dish_id, rating, comment)
        VALUES (%s, %s, %s, %s)
        ON DUPLICATE KEY UPDATE rating=VALUES(rating), comment=VALUES(comment)
    """, rating)

connection.commit()
print(f"成功导入 {len(ratings)} 条评分数据")

cursor.close()
connection.close()
```

运行脚本：
```bash
cd backend
python scripts/import_data.py
```

### 方式四：通过API添加（用于自动化）

#### 使用cURL添加评分

```bash
# 先登录获取Token
TOKEN=$(curl -X POST http://localhost:5000/api/v1/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"password"}' | jq -r '.token')

# 添加评分
curl -X POST http://localhost:5000/api/v1/ratings \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "dish_id": 1,
    "rating": 5,
    "comment": "很好吃"
  }'
```

---

## 📊 数据结构说明

### dishes 表结构

```sql
CREATE TABLE dishes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,           -- 菜品名称
    category VARCHAR(50),                 -- 分类（荤菜/素菜/汤类/主食）
    price DECIMAL(10, 2),                 -- 价格
    description TEXT,                     -- 描述
    image_url VARCHAR(255),               -- 图片URL
    nutrition_info JSON,                  -- 营养信息（可选）
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

**字段说明：**
- `name`: 菜品名称（必填）
- `category`: 分类，影响特征编码（推荐填写）
- `price`: 价格，用于价格区间特征
- `description`: 描述，用于TF-IDF文本特征
- `image_url`: 图片URL（可选）

### ratings 表结构

```sql
CREATE TABLE ratings (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,                 -- 用户ID
    dish_id INT NOT NULL,                 -- 菜品ID
    rating INT NOT NULL CHECK (rating >= 1 AND rating <= 5),  -- 评分(1-5)
    comment TEXT,                         -- 评论（可选）
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY unique_user_dish (user_id, dish_id)  -- 每个用户对每个菜品只能评一次
);
```

**字段说明：**
- `user_id`: 用户ID（必填）
- `dish_id`: 菜品ID（必填）
- `rating`: 评分1-5（必填），用于协同过滤训练
- `comment`: 评论（可选）

### consumption_records 表结构

```sql
CREATE TABLE consumption_records (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,                 -- 用户ID
    dish_id INT NOT NULL,                 -- 菜品ID
    consumption_time TIMESTAMP NOT NULL,  -- 消费时间
    price DECIMAL(10, 2),                 -- 消费价格
    rating INT CHECK (rating >= 1 AND rating <= 5),  -- 评分（可选）
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (dish_id) REFERENCES dishes(id)
);
```

**字段说明：**
- 消费记录也会用于训练（如果包含评分）
- 可以记录用户的实际消费行为

---

## 📝 数据导入示例

### 示例1：添加10道菜品

```sql
INSERT INTO dishes (name, category, price, description) VALUES
('红烧肉', '荤菜', 12.00, '经典红烧肉，肥而不腻'),
('麻婆豆腐', '素菜', 8.00, '川味麻婆豆腐，麻辣鲜香'),
('西红柿鸡蛋', '素菜', 10.00, '家常西红柿炒鸡蛋'),
('宫保鸡丁', '荤菜', 15.00, '经典川菜宫保鸡丁'),
('糖醋里脊', '荤菜', 14.00, '酸甜可口的糖醋里脊'),
('地三鲜', '素菜', 9.00, '东北名菜地三鲜'),
('鱼香肉丝', '荤菜', 13.00, '川菜鱼香肉丝'),
('青椒土豆丝', '素菜', 7.00, '清爽青椒土豆丝'),
('回锅肉', '荤菜', 16.00, '四川回锅肉'),
('酸辣土豆丝', '素菜', 8.00, '开胃酸辣土豆丝');
```

### 示例2：为多个用户添加评分

```sql
-- 假设已有用户ID: 1, 2, 3
-- 假设已有菜品ID: 1-10

-- 用户1的评分
INSERT INTO ratings (user_id, dish_id, rating, comment) VALUES
(1, 1, 5, '很好吃'),
(1, 2, 4, '还不错'),
(1, 3, 5, '推荐'),
(1, 4, 3, '一般'),
(1, 5, 5, '非常喜欢');

-- 用户2的评分
INSERT INTO ratings (user_id, dish_id, rating, comment) VALUES
(2, 1, 4, '不错'),
(2, 2, 5, '很喜欢'),
(2, 6, 5, '好吃'),
(2, 7, 4, '可以'),
(2, 8, 3, '一般');

-- 用户3的评分
INSERT INTO ratings (user_id, dish_id, rating, comment) VALUES
(3, 3, 5, '最爱'),
(3, 4, 5, '推荐'),
(3, 9, 4, '不错'),
(3, 10, 5, '很好');
```

### 示例3：批量生成测试数据

创建脚本 `backend/scripts/generate_test_data.py`：

```python
import random
import sys
import os
from datetime import datetime, timedelta

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
from utils.database import db

connection = db.get_connection()
cursor = connection.cursor()

# 获取所有用户ID
cursor.execute("SELECT id FROM users")
user_ids = [row[0] for row in cursor.fetchall()]

# 获取所有菜品ID
cursor.execute("SELECT id FROM dishes")
dish_ids = [row[0] for row in cursor.fetchall()]

if not user_ids or not dish_ids:
    print("错误：需要先有用户和菜品数据")
    exit(1)

# 为每个用户随机评分
total_ratings = 0
for user_id in user_ids:
    # 每个用户随机评分5-15道菜品
    num_ratings = random.randint(5, 15)
    rated_dishes = random.sample(dish_ids, min(num_ratings, len(dish_ids)))
    
    for dish_id in rated_dishes:
        rating = random.randint(1, 5)  # 随机评分1-5
        comment = random.choice(['很好吃', '还不错', '一般', '推荐', '非常喜欢', ''])
        
        try:
            cursor.execute("""
                INSERT INTO ratings (user_id, dish_id, rating, comment)
                VALUES (%s, %s, %s, %s)
                ON DUPLICATE KEY UPDATE rating=VALUES(rating)
            """, (user_id, dish_id, rating, comment))
            total_ratings += 1
        except Exception as e:
            print(f"跳过重复评分: user_id={user_id}, dish_id={dish_id}")

connection.commit()
print(f"成功生成 {total_ratings} 条评分数据")

cursor.close()
connection.close()
```

运行：
```bash
cd backend
python scripts/generate_test_data.py
```

---

## 🔄 添加数据后的操作

### 1. 重新进行数据预处理

```bash
# 本地环境
cd backend
source venv/bin/activate
python data/preprocessor.py

# Docker环境
docker-compose exec backend python data/preprocessor.py
```

**说明：** 数据预处理会：
- 重新加载所有菜品和评分数据
- 重新计算特征（平均评分、评分总数等）
- 重新进行TF-IDF向量化和PCA降维
- 更新 `dish_features` 表

### 2. 重新训练模型

```bash
# 本地环境
python train_model.py

# Docker环境
docker-compose exec backend python train_model.py
```

**说明：** 重新训练会：
- 使用新的评分数据训练协同过滤模型
- 使用新的特征数据训练内容推荐模型
- 更新模型文件（`cf_model.pkl`, `cb_model.pkl`）

### 3. 验证推荐效果

```bash
# 登录并获取Token
curl -X POST http://localhost:5000/api/v1/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"password"}'

# 获取推荐（使用返回的Token）
curl -X GET http://localhost:5000/api/v1/recommendations \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 💡 最佳实践

### 1. 数据质量

- **评分数据**：确保有足够的评分数据（至少每个用户5条以上）
- **菜品多样性**：添加不同类别、不同价格的菜品
- **数据真实性**：尽量使用真实的用户评分数据

### 2. 数据量建议

- **最少数据量**：
  - 菜品：至少20道
  - 用户：至少10个
  - 评分：每个用户至少5条

- **推荐数据量**：
  - 菜品：50-100道
  - 用户：100-500个
  - 评分：每个用户10-50条

### 3. 定期更新

- **新菜品**：添加新菜品后，运行数据预处理
- **新评分**：积累一定数量后，重新训练模型
- **建议频率**：每周或每月重新训练一次

---

## 📚 相关文档

- [系统架构说明.md](./系统架构说明.md) - 了解系统架构
- [前后端连接说明.md](./前后端连接说明.md) - 了解API接口
- [README.md](./README.md) - 项目总览

---

**记住：训练数据来自数据库，不是写死的！添加数据后记得重新训练模型！**

