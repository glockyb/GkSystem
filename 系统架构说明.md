# 系统架构说明文档

## 📋 目录
1. [工程思路](#工程思路)
2. [系统架构](#系统架构)
3. [前后端连接](#前后端连接)
4. [数据流程](#数据流程)
5. [训练模型位置和流程](#训练模型位置和流程)

---

## 🎯 工程思路

### 整体设计理念

本项目采用**前后端分离**的架构，使用**微服务化**的思想，通过**RESTful API**进行通信。推荐系统采用**混合推荐算法**，结合协同过滤和内容推荐，提供个性化菜品推荐。

### 技术选型理由

1. **前端：Vue 3 + Element Plus**
   - 响应式框架，用户体验好
   - 组件化开发，代码复用性高
   - Element Plus提供丰富的UI组件

2. **后端：Flask + Python**
   - 轻量级框架，易于开发和部署
   - Python丰富的机器学习库（scikit-learn, pandas等）
   - RESTful API设计，接口清晰

3. **数据库：MySQL + Redis**
   - MySQL：存储结构化数据（用户、菜品、评分等）
   - Redis：缓存推荐结果，提高响应速度

4. **机器学习：scikit-learn + Surprise**
   - scikit-learn：特征工程、内容推荐
   - Surprise：协同过滤推荐

5. **部署：Docker + Docker Compose**
   - 容器化部署，环境一致
   - 一键启动，便于维护

---

## 🏗️ 系统架构

### 架构图

```
┌─────────────────────────────────────────────────────┐
│                     用户浏览器                        │
└──────────────────┬──────────────────────────────────┘
                   │ HTTP/HTTPS
                   ▼
┌─────────────────────────────────────────────────────┐
│                    前端层 (Vue 3)                    │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐   │
│  │  Login     │  │   Home     │  │  Profile   │   │
│  └────────────┘  └────────────┘  └────────────┘   │
└──────────────────┬──────────────────────────────────┘
                   │ RESTful API (/api/v1/*)
                   ▼
┌─────────────────────────────────────────────────────┐
│                   Nginx 反向代理                      │
│  ┌──────────────────────────────────────────────┐  │
│  │  /         →  前端静态资源                     │  │
│  │  /api/*   →  后端 Flask 服务                  │  │
│  └──────────────────────────────────────────────┘  │
└──────────────────┬──────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────┐
│                后端 API 层 (Flask)                    │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐   │
│  │   Auth     │  │  Dishes    │  │ Recommend  │   │
│  │   API      │  │   API      │  │   API      │   │
│  └────────────┘  └────────────┘  └────────────┘   │
└──────────────────┬──────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────┐
│              业务逻辑层 (Python)                      │
│  ┌──────────────────────────────────────────────┐  │
│  │          推荐算法模块 (Recommender)           │  │
│  │  ┌──────────────┐      ┌──────────────┐     │  │
│  │  │ 协同过滤 (CF)│      │ 内容推荐 (CB) │     │  │
│  │  └──────────────┘      └──────────────┘     │  │
│  │  ┌──────────────────────────────────────┐   │  │
│  │  │        混合推荐 (Hybrid)              │   │  │
│  │  └──────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────┘  │
└──────────────────┬──────────────────────────────────┘
                   │
         ┌─────────┴─────────┐
         ▼                   ▼
┌─────────────────┐  ┌─────────────────┐
│   MySQL 数据库   │  │  Redis 缓存     │
│  - 用户数据      │  │  - 推荐结果缓存  │
│  - 菜品数据      │  │  - Session      │
│  - 评分数据      │  │                 │
│  - 消费记录      │  │                 │
│  - 特征数据      │  │                 │
└─────────────────┘  └─────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────┐
│              模型训练层 (Offline)                     │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐   │
│  │ 数据预处理  │  │ 特征工程   │  │ 模型训练   │   │
│  │ (Preprocess)│  │ (Features) │  │ (Train)    │   │
│  └────────────┘  └────────────┘  └────────────┘   │
│                      │                               │
│                      ▼                               │
│         ┌────────────────────────┐                  │
│         │  训练好的模型文件 (.pkl) │                  │
│         └────────────────────────┘                  │
└─────────────────────────────────────────────────────┘
```

### 目录结构

```
GkSystem/
├── frontend/              # 前端项目
│   ├── src/
│   │   ├── api/          # API 接口封装
│   │   ├── components/   # Vue 组件
│   │   ├── views/        # 页面视图
│   │   ├── router/       # 路由配置
│   │   ├── store/        # 状态管理 (Pinia)
│   │   └── styles/       # 全局样式
│   ├── Dockerfile
│   └── nginx.conf        # Nginx 配置
│
├── backend/              # 后端项目
│   ├── api/              # API 路由 (蓝图)
│   │   ├── auth.py       # 认证 API
│   │   ├── dishes.py     # 菜品 API
│   │   ├── ratings.py    # 评分 API
│   │   └── recommendations.py  # 推荐 API
│   ├── models/           # 模型层
│   │   └── recommender.py # 推荐算法
│   ├── data/             # 数据处理
│   │   └── preprocessor.py # 数据预处理
│   ├── database/         # 数据库脚本
│   │   └── init.sql      # 初始化 SQL
│   ├── utils/            # 工具类
│   │   └── database.py   # 数据库连接
│   ├── app.py            # Flask 应用入口
│   ├── config.py         # 配置文件
│   └── train_model.py    # 模型训练脚本
│
└── docker-compose.yml    # Docker 编排文件
```

---

## 🔗 前后端连接

### 连接方式

1. **开发环境**
   - 前端：Vite 开发服务器（端口 8080）
   - 后端：Flask 开发服务器（端口 5000）
   - 连接：Vite 代理配置（`vite.config.js`）

```javascript
// frontend/vite.config.js
server: {
  port: 8080,
  proxy: {
    '/api': {
      target: 'http://localhost:5000',
      changeOrigin: true
    }
  }
}
```

2. **生产环境（Docker）**
   - 前端：Nginx 静态服务器（容器内端口 80，映射到 8080）
   - 后端：Flask 服务（端口 5000）
   - 连接：Nginx 反向代理（`nginx.conf`）

```nginx
# frontend/nginx.conf
location /api {
    proxy_pass http://backend:5000/api;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}
```

### API 通信流程

```
前端 (Vue)                         后端 (Flask)
  │                                   │
  │ 1. 发起请求                        │
  ├─────────────────────────────────>│
  │    POST /api/v1/login            │
  │    { username, password }         │
  │                                   │
  │                                   │ 2. 处理请求
  │                                   │    - 验证用户
  │                                   │    - 生成 JWT Token
  │                                   │
  │ 3. 返回响应                        │
  │<──────────────────────────────────┤
  │    { token, user_id, ... }        │
  │                                   │
  │ 4. 存储 Token                     │
  │    localStorage.setItem('token')  │
  │                                   │
  │ 5. 后续请求带 Token               │
  ├─────────────────────────────────>│
  │    GET /api/v1/recommendations   │
  │    Header: Authorization: Bearer  │
  │                                   │
  │                                   │ 6. 验证 Token
  │                                   │    - 解析 JWT
  │                                   │    - 获取 user_id
  │                                   │    - 调用推荐算法
  │                                   │
  │ 7. 返回推荐结果                    │
  │<──────────────────────────────────┤
  │    { dishes: [...] }              │
```

### API 接口列表

#### 1. 认证相关 (`/api/v1`)

| 方法 | 路径 | 说明 | 是否需要Token |
|------|------|------|--------------|
| POST | `/register` | 用户注册 | 否 |
| POST | `/login` | 用户登录 | 否 |
| GET | `/profile` | 获取用户信息 | 是 |

#### 2. 菜品相关

| 方法 | 路径 | 说明 | 是否需要Token |
|------|------|------|--------------|
| GET | `/dishes` | 获取菜品列表 | 否 |
| GET | `/dishes/:id` | 获取菜品详情 | 否 |
| GET | `/dishes/categories` | 获取分类列表 | 否 |

#### 3. 推荐相关

| 方法 | 路径 | 说明 | 是否需要Token |
|------|------|------|--------------|
| GET | `/recommendations` | 获取个性化推荐 | 是 |

#### 4. 评分相关

| 方法 | 路径 | 说明 | 是否需要Token |
|------|------|------|--------------|
| POST | `/ratings` | 创建评分 | 是 |
| GET | `/ratings/:dish_id` | 获取菜品评分 | 否 |
| GET | `/history` | 获取评分历史 | 是 |

### 前端 API 封装

前端使用 `axios` 封装 API 调用：

```javascript
// frontend/src/api/index.js
import axios from 'axios'

const api = axios.create({
  baseURL: '/api/v1',  // 统一前缀
  timeout: 10000
})

// 请求拦截器：自动添加 Token
api.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器：处理错误
api.interceptors.response.use(
  response => response.data,  // 直接返回 data
  error => {
    if (error.response?.status === 401) {
      // Token 过期，跳转登录
      localStorage.removeItem('token')
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)
```

---

## 📊 数据流程

### 数据流向图

```
用户操作
   │
   ├─→ 浏览菜品 → API → MySQL (dishes表)
   │
   ├─→ 评分菜品 → API → MySQL (ratings表) → 触发推荐更新
   │
   └─→ 查看推荐 → API → Redis缓存 → 推荐算法 → MySQL (特征数据) → 返回结果
```

### 推荐系统数据流程

1. **数据采集**
   - 用户评分 → `ratings` 表
   - 消费记录 → `consumption_records` 表
   - 菜品信息 → `dishes` 表

2. **数据预处理**（离线）
   - 加载数据（从MySQL）
   - 数据清洗（处理缺失值、异常值）
   - 特征工程（提取12个特征）
   - TF-IDF向量化（文本特征）
   - PCA降维（32维）
   - 保存特征 → `dish_features` 表

3. **模型训练**（离线）
   - 加载评分数据
   - 训练协同过滤模型（SVD）
   - 训练内容推荐模型（KNN）
   - 保存模型文件（`.pkl`）

4. **在线推荐**（实时）
   - 用户请求推荐
   - 检查Redis缓存
   - 调用推荐算法
   - 合并协同过滤和内容推荐结果
   - 缓存推荐结果（1小时）
   - 返回给前端

---

## 🤖 训练模型位置和流程

### 模型位置

**训练模型代码位置：**

```
backend/
├── models/
│   └── recommender.py      # 推荐算法实现
│       ├── CollaborativeFiltering  # 协同过滤
│       ├── ContentBasedRecommender # 内容推荐
│       └── HybridRecommender      # 混合推荐
│
├── data/
│   └── preprocessor.py     # 数据预处理
│
└── train_model.py          # 训练入口脚本
```

**训练好的模型文件位置：**

```
backend/models/
├── cf_model.pkl           # 协同过滤模型
└── cb_model.pkl           # 内容推荐模型
```

### 训练数据来源

**训练数据不是写死的！** 数据从 **MySQL 数据库** 动态加载：

1. **评分数据**：从 `ratings` 和 `consumption_records` 表加载
2. **菜品数据**：从 `dishes` 表加载
3. **特征数据**：从 `dish_features` 表加载（由数据预处理生成）

### 训练流程详解

#### 第一步：数据预处理

**脚本：** `backend/data/preprocessor.py`

**流程：**
```python
1. 从MySQL加载数据
   - consumption_records (消费记录)
   - ratings (评分)
   - dishes (菜品)

2. 数据清洗
   - 处理缺失值
   - 剔除异常值（3σ原则）

3. 特征工程
   - 类别编码（荤菜/素菜/汤类/主食）
   - 价格区间（<8, 8-12, 12-15, >15）
   - 平均评分
   - 评分总数
   - 价格
   - 名称和描述的TF-IDF向量

4. 特征归一化
   - MinMaxScaler归一化

5. PCA降维
   - 降到32维

6. 保存特征到数据库
   - 保存到 dish_features 表
```

#### 第二步：模型训练

**脚本：** `backend/train_model.py`

**流程：**
```python
1. 调用数据预处理
   preprocessor.process_all()

2. 训练协同过滤模型
   - 加载评分数据（user_id, dish_id, rating）
   - 使用Surprise库的SVD算法
   - 矩阵分解，学习用户和菜品的潜在因子
   - 保存模型到 cf_model.pkl

3. 训练内容推荐模型
   - 加载菜品特征（从dish_features表）
   - 使用KNN算法（余弦相似度）
   - 建立菜品相似度索引
   - 保存模型到 cb_model.pkl
```

#### 第三步：模型使用

**代码：** `backend/models/recommender.py`

**推荐流程：**
```python
1. 用户请求推荐
   recommender.recommend(user_id, n=10)

2. 混合推荐
   - 协同过滤：根据用户历史评分推荐
   - 内容推荐：根据用户喜欢的菜品特征推荐相似菜品
   - 加权合并：CF权重60% + CB权重40%

3. 返回推荐结果
   - 按分数排序
   - 返回Top-N菜品ID
```

### 如何运行训练

#### 方式一：手动训练

```bash
# 进入后端目录
cd backend

# 激活虚拟环境
source venv/bin/activate

# 数据预处理
python data/preprocessor.py

# 训练模型
python train_model.py
```

#### 方式二：Docker环境训练

```bash
# 数据预处理
docker-compose exec backend python data/preprocessor.py

# 训练模型
docker-compose exec backend python train_model.py
```

#### 方式三：使用启动脚本

```bash
# 后端启动脚本会自动检查并训练模型（如果模型不存在）
./启动后端.sh
```

### 训练时机

1. **首次启动**：系统自动检测，如果模型不存在则训练
2. **手动触发**：运行 `train_model.py`
3. **定时任务**：可以配置定时训练（在 `config.py` 中配置）

### 模型更新

当有新的评分数据时，可以重新训练模型以更新推荐效果：

```bash
# 重新训练模型
python train_model.py

# 或使用脚本
cd backend
python train_model.py
```

---

## 📝 总结

### 关键点

1. **数据不是写死的**：所有数据来自MySQL数据库
2. **模型训练在离线层**：`backend/models/` 和 `backend/data/`
3. **推荐服务在在线层**：`backend/api/recommendations.py`
4. **前后端通过RESTful API连接**：统一使用 `/api/v1` 前缀
5. **使用JWT Token进行身份认证**

### 数据流向总结

```
用户操作 → 前端 (Vue) → API请求 → 后端 (Flask) → 推荐算法 → 数据库 (MySQL/Redis) → 返回结果
                                                                        ↑
                                                           训练数据从这里加载
```

---

**详细说明请查看：**
- [训练数据说明.md](./训练数据说明.md) - 如何添加训练数据
- [前后端连接说明.md](./前后端连接说明.md) - 前后端详细连接方式

