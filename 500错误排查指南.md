# 500 错误排查指南

## 🔍 快速诊断

运行诊断脚本：

```bash
cd ~/GkSystem
chmod +x diagnose-500-error.sh
sudo ./diagnose-500-error.sh
```

## 📋 常见原因和解决方案

### 问题1：后端服务未运行

**症状：** 500 错误，后端 API 无响应

**检查：**
```bash
sudo systemctl status canteen-backend
```

**解决：**
```bash
# 启动服务
sudo systemctl start canteen-backend

# 查看日志
sudo journalctl -u canteen-backend -n 50

# 如果启动失败，查看详细错误
sudo journalctl -u canteen-backend -f
```

### 问题2：后端依赖缺失

**症状：** 后端服务启动失败，日志显示 `ModuleNotFoundError`

**检查：**
```bash
cd ~/GkSystem/backend
source venv/bin/activate
python3 -c "import pandas, numpy, flask, pymysql, redis; print('OK')"
```

**解决：**
```bash
cd ~/GkSystem
sudo ./fix-backend-deps-manual.sh
```

### 问题3：数据库连接失败

**症状：** 后端日志显示数据库连接错误

**检查：**
```bash
# 检查 MySQL 是否运行
sudo systemctl status mysql

# 测试数据库连接
mysql -u root -e "USE canteen_recommendation; SELECT 1;"
```

**解决：**
```bash
# 启动 MySQL
sudo systemctl start mysql

# 检查数据库是否存在
mysql -u root -e "SHOW DATABASES;"

# 如果数据库不存在，创建
mysql -u root <<EOF
CREATE DATABASE IF NOT EXISTS canteen_recommendation CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EOF
```

### 问题4：Nginx 配置错误

**症状：** Nginx 错误日志显示配置问题

**检查：**
```bash
# 测试配置
sudo nginx -t

# 查看错误日志
sudo tail -f /var/log/nginx/error.log
```

**解决：**
```bash
# 检查配置文件
sudo cat /etc/nginx/sites-available/canteen

# 修复配置后重新加载
sudo nginx -t
sudo systemctl reload nginx
```

### 问题5：前端文件不存在或路径错误

**症状：** Nginx 错误日志显示 404 或文件不存在

**检查：**
```bash
# 检查前端文件
ls -la ~/GkSystem/frontend/dist/

# 检查 Nginx 配置中的路径
sudo grep -r "root" /etc/nginx/sites-available/canteen
```

**解决：**
```bash
# 重新构建前端
cd ~/GkSystem/frontend
npm run build

# 检查路径是否正确
sudo nano /etc/nginx/sites-available/canteen
# 确保 root 路径正确
```

### 问题6：端口被占用或防火墙阻止

**症状：** 无法访问后端 API

**检查：**
```bash
# 检查端口占用
sudo netstat -tulpn | grep 5000
sudo ss -tulpn | grep 5000

# 检查防火墙
sudo ufw status
```

**解决：**
```bash
# 如果端口被占用，停止占用进程
sudo kill <PID>

# 配置防火墙
sudo ufw allow 80/tcp
sudo ufw allow 5000/tcp
```

## 🔧 完整排查步骤

### 步骤1：检查服务状态

```bash
# 后端服务
sudo systemctl status canteen-backend

# Nginx
sudo systemctl status nginx

# MySQL
sudo systemctl status mysql

# Redis
sudo systemctl status redis-server
```

### 步骤2：查看日志

```bash
# 后端日志
sudo journalctl -u canteen-backend -n 50 -f

# Nginx 错误日志
sudo tail -f /var/log/nginx/error.log

# Nginx 访问日志
sudo tail -f /var/log/nginx/canteen-access.log
```

### 步骤3：测试本地连接

```bash
# 测试后端 API
curl http://localhost:5000/health

# 测试前端
curl http://localhost/

# 测试 Nginx 代理
curl http://localhost/api/v1/
```

### 步骤4：检查配置

```bash
# Nginx 配置
sudo nginx -t
sudo cat /etc/nginx/sites-available/canteen

# 后端配置
cd ~/GkSystem/backend
cat .env
cat config.py
```

### 步骤5：手动测试后端

```bash
cd ~/GkSystem/backend
source venv/bin/activate

# 测试导入
python3 -c "from app import app; print('OK')"

# 手动启动（测试）
python3 app.py
# 在另一个终端测试: curl http://localhost:5000/health
```

## 🚀 快速修复命令

### 完全重启所有服务

```bash
# 重启后端
sudo systemctl restart canteen-backend

# 重启 Nginx
sudo systemctl restart nginx

# 等待几秒
sleep 5

# 测试
curl http://localhost:5000/health
curl http://localhost/
```

### 重新部署后端

```bash
cd ~/GkSystem/backend

# 检查虚拟环境
source venv/bin/activate

# 验证依赖
python3 -c "import pandas, numpy, flask, pymysql, redis; print('OK')"

# 如果依赖缺失，重新安装
pip3 install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 重启服务
sudo systemctl restart canteen-backend
```

### 重新构建前端

```bash
cd ~/GkSystem/frontend

# 清理并重新构建
rm -rf dist node_modules
npm install
npm run build

# 检查文件
ls -la dist/

# 重启 Nginx
sudo systemctl reload nginx
```

## 📊 诊断检查清单

- [ ] 后端服务运行中
- [ ] 后端 API 可访问（localhost:5000）
- [ ] Nginx 运行中
- [ ] Nginx 配置正确
- [ ] 前端文件存在（dist/index.html）
- [ ] 数据库连接正常
- [ ] Redis 连接正常（可选）
- [ ] 所有 Python 依赖已安装
- [ ] 端口未被占用
- [ ] 防火墙已配置

## 🔍 详细错误信息获取

### 查看完整后端日志

```bash
sudo journalctl -u canteen-backend --since "10 minutes ago" | less
```

### 查看 Nginx 完整错误日志

```bash
sudo tail -100 /var/log/nginx/error.log
```

### 实时监控

```bash
# 在一个终端查看后端日志
sudo journalctl -u canteen-backend -f

# 在另一个终端查看 Nginx 日志
sudo tail -f /var/log/nginx/error.log

# 在第三个终端测试访问
curl -v http://localhost/
```

## 📚 相关脚本

- `diagnose-500-error.sh` - 自动诊断脚本
- `fix-backend-deps-manual.sh` - 修复后端依赖
- `部署后问题排查.md` - 详细排查文档

---

**运行诊断脚本获取详细错误信息！**

