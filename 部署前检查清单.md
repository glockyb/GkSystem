# 部署前检查清单

## ⚠️ 重要提示

如果项目是从 **macOS 或 Windows** 复制到 **Linux 服务器**，请务必注意：

### 1. 不要复制 node_modules 目录

**原因：**
- `node_modules` 包含平台特定的二进制文件（如 esbuild）
- macOS 的二进制文件不能在 Linux 上运行
- 会导致构建失败

**解决方法：**
- ✅ 只复制源代码，不复制 `node_modules`
- ✅ 在 Linux 服务器上运行 `npm install` 重新安装

### 2. 上传项目时的正确做法

#### 方法一：使用 Git（推荐）

```bash
# 在本地
git add .
git commit -m "准备部署"
git push

# 在服务器上
git clone <your-repo-url>
cd GkSystem
# node_modules 不会被克隆（在 .gitignore 中）
```

#### 方法二：使用 scp（排除 node_modules）

```bash
# 在本地执行
cd ~/GkSystem

# 创建临时目录（排除 node_modules）
rsync -av --exclude 'node_modules' \
  --exclude '.git' \
  --exclude 'backend/venv' \
  --exclude 'frontend/dist' \
  . /tmp/GkSystem-clean/

# 上传清理后的项目
scp -r /tmp/GkSystem-clean username@server-ip:~/
```

#### 方法三：使用 tar（排除 node_modules）

```bash
# 在本地执行
cd ~/GkSystem
tar --exclude='node_modules' \
    --exclude='.git' \
    --exclude='backend/venv' \
    --exclude='frontend/dist' \
    -czf GkSystem-clean.tar.gz .

# 上传
scp GkSystem-clean.tar.gz username@server-ip:~/

# 在服务器上解压
ssh username@server-ip
tar -xzf GkSystem-clean.tar.gz
```

### 3. 部署脚本自动处理

`deploy-cloud-server.sh` 脚本会自动：
- ✅ 检测并删除跨平台的 `node_modules`
- ✅ 在 Linux 平台上重新安装依赖
- ✅ 确保所有二进制文件都是 Linux 版本

### 4. 如果已经复制了 node_modules

如果已经复制了包含 macOS/Windows node_modules 的项目：

```bash
# 在服务器上执行
cd ~/GkSystem/frontend
rm -rf node_modules package-lock.json
npm install

# 或使用修复脚本
cd ~/GkSystem
./fix-frontend-build.sh
```

## 📋 完整检查清单

### 上传前检查

- [ ] 项目代码已提交到 Git
- [ ] `.gitignore` 包含 `node_modules`
- [ ] 不包含 `node_modules` 目录
- [ ] 不包含 `backend/venv` 目录
- [ ] 不包含 `frontend/dist` 目录（可选）

### 服务器上检查

- [ ] 项目已上传到服务器
- [ ] 没有 `node_modules` 目录（或已删除）
- [ ] Node.js 已安装（18.x 推荐）
- [ ] Python 3.8+ 已安装
- [ ] MySQL 已安装
- [ ] Redis 已安装

### 部署后检查

- [ ] 后端服务运行正常
- [ ] 前端构建成功
- [ ] Nginx 配置正确
- [ ] 可以通过公网 IP 访问

## 🔧 常见问题

### Q: 为什么不能复制 node_modules？

**A:** `node_modules` 包含平台特定的二进制文件：
- macOS: `@esbuild/darwin-x64`
- Linux: `@esbuild/linux-x64`
- Windows: `@esbuild/win32-x64`

这些二进制文件不能跨平台使用。

### Q: 如何确认 node_modules 是跨平台的？

**A:** 检查 esbuild 目录：
```bash
ls node_modules/@esbuild/
# 如果看到 darwin-x64 或 win32-x64，说明是跨平台的
```

### Q: 部署脚本会自动处理吗？

**A:** 是的，`deploy-cloud-server.sh` 会自动：
1. 检测 node_modules
2. 删除跨平台的 node_modules
3. 重新安装 Linux 版本的依赖

## 📚 相关文档

- [Ubuntu20.04部署说明.md](./Ubuntu20.04部署说明.md)
- [云主机非Docker部署指南.md](./云主机非Docker部署指南.md)
- [fix-frontend-build.sh](./fix-frontend-build.sh) - 前端构建修复脚本

---

**记住：永远不要在 macOS/Windows 和 Linux 之间复制 node_modules！**

