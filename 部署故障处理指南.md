# 部署故障处理指南

## 🚨 常见问题：部署中断和网络超时

### 问题描述

在远端服务器上运行 `deploy-remote.sh` 时，如果遇到：
```
Error response from daemon: failed to resolve reference "docker.io/library/redis:7-alpine": 
failed to do request: Head "https://registry-1.docker.io/v2/library/redis/manifests/7-alpine": 
dial tcp 69.63.186.31:443: i/o timeout
```

这表示**无法连接到 Docker Hub 拉取镜像**。

### 问题原因

1. **网络超时**：服务器无法访问 Docker Hub（常见于中国服务器）
2. **连接中断**：部署过程中 SSH 连接中断，导致容器状态异常
3. **防火墙限制**：防火墙阻止了 Docker Hub 的连接
4. **残留容器**：上次中断后留下不完整的容器

## ✅ 快速解决方案

### 方案一：使用修复脚本（推荐）

在**服务器上**运行修复脚本：

```bash
cd /path/to/GkSystem

# 运行修复脚本
./fix-deploy-issue.sh
```

这个脚本会自动：
- ✅ 清理残留容器和资源
- ✅ 配置 Docker 镜像加速器（中国镜像源）
- ✅ 重启 Docker 服务
- ✅ 测试网络连接
- ✅ 可选预拉取镜像

### 方案二：手动修复步骤

如果修复脚本不可用，按以下步骤操作：

#### 1. 清理残留容器

```bash
cd /path/to/GkSystem

# 停止所有容器
docker-compose down || docker compose down

# 手动停止残留容器（如果上面失败）
docker stop canteen-mysql canteen-redis canteen-backend canteen-frontend 2>/dev/null || true
docker rm canteen-mysql canteen-redis canteen-backend canteen-frontend 2>/dev/null || true

# 清理网络
docker network prune -f
```

#### 2. 配置 Docker 镜像加速器

```bash
# 创建配置目录
sudo mkdir -p /etc/docker

# 创建配置文件
sudo tee /etc/docker/daemon.json > /dev/null <<EOF
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com",
    "https://mirror.baidubce.com"
  ]
}
EOF

# 重启 Docker
sudo systemctl daemon-reload
sudo systemctl restart docker

# 等待 Docker 启动
sleep 5

# 验证配置
docker info | grep "Registry Mirrors"
```

#### 3. 重新部署

```bash
./deploy-remote.sh
```

## 🔧 详细故障排查

### 步骤1：检查当前状态

```bash
# 查看容器状态
docker ps -a

# 查看 Docker Compose 状态
docker-compose ps || docker compose ps

# 查看 Docker 日志
sudo journalctl -u docker.service -n 50
```

### 步骤2：检查网络连接

```bash
# 测试 Docker Hub 连接
ping -c 3 registry-1.docker.io

# 测试镜像加速器连接
ping -c 3 docker.mirrors.ustc.edu.cn

# 测试 HTTPS 连接
curl -I https://docker.mirrors.ustc.edu.cn
```

### 步骤3：手动拉取镜像

```bash
# 配置镜像加速器后，尝试拉取镜像
docker pull mysql:8.0
docker pull redis:7-alpine
docker pull nginx:alpine
```

如果仍然超时，检查防火墙设置：

```bash
# Ubuntu/Debian
sudo ufw status
sudo ufw allow 443/tcp

# CentOS/RHEL
sudo firewall-cmd --list-all
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

## 📝 部署中断后的完整处理流程

### 场景：部署过程中 SSH 连接中断

#### 第一步：重新连接服务器

```bash
ssh username@your-server-ip
cd /path/to/GkSystem
```

#### 第二步：运行修复脚本

```bash
./fix-deploy-issue.sh
```

脚本会自动处理所有问题。

#### 第三步：如果修复脚本不可用，手动修复

```bash
# 1. 清理所有残留资源
docker-compose down 2>/dev/null || docker compose down 2>/dev/null || true
docker stop $(docker ps -aq) 2>/dev/null || true
docker rm $(docker ps -aq) 2>/dev/null || true
docker network prune -f

# 2. 配置镜像加速器
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json > /dev/null <<EOF
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com",
    "https://mirror.baidubce.com"
  ]
}
EOF

# 3. 重启 Docker
sudo systemctl daemon-reload
sudo systemctl restart docker
sleep 5

# 4. 验证配置
docker info | grep "Registry Mirrors"

# 5. 重新部署
./deploy-remote.sh
```

## 💡 预防措施

### 使用 screen 或 tmux 保持会话

#### 使用 screen（推荐）

```bash
# 安装 screen（如果没有）
sudo apt install screen

# 创建新会话
screen -S deploy

# 在会话中运行部署脚本
./deploy-remote.sh

# 断开连接：按 Ctrl+A，然后按 D
# 重新连接：screen -r deploy

# 查看所有会话：screen -ls
```

#### 使用 tmux

```bash
# 安装 tmux（如果没有）
sudo apt install tmux

# 创建新会话
tmux new -s deploy

# 在会话中运行部署脚本
./deploy-remote.sh

# 断开连接：按 Ctrl+B，然后按 D
# 重新连接：tmux attach -t deploy
```

### 使用 nohup 后台运行

```bash
# 后台运行部署脚本
nohup ./deploy-remote.sh > deploy.log 2>&1 &

# 查看日志
tail -f deploy.log

# 查看进程
ps aux | grep deploy-remote.sh
```

## 🔍 检查清单

部署前检查：

- [ ] Docker 已安装并运行
- [ ] Docker Compose 已安装
- [ ] 网络连接正常
- [ ] 防火墙已配置（允许必要端口）
- [ ] 磁盘空间充足（至少 10GB）
- [ ] 内存充足（至少 2GB）

部署后检查：

- [ ] 所有容器正常运行
- [ ] 端口未被占用
- [ ] 数据库初始化成功
- [ ] 模型训练完成
- [ ] 前端可以访问
- [ ] 后端 API 正常响应

## 📋 常用命令

### 查看服务状态

```bash
# 查看容器状态
docker-compose ps || docker compose ps

# 查看日志
docker-compose logs -f || docker compose logs -f

# 查看特定服务日志
docker-compose logs -f backend || docker compose logs -f backend
```

### 重启服务

```bash
# 重启所有服务
docker-compose restart || docker compose restart

# 重启特定服务
docker-compose restart backend || docker compose restart backend

# 停止所有服务
docker-compose down || docker compose down

# 停止并删除 volumes（⚠️ 会删除数据）
docker-compose down -v || docker compose down -v
```

### 清理资源

```bash
# 清理未使用的容器、网络、镜像
docker system prune -a

# 清理 volumes（⚠️ 会删除数据）
docker volume prune

# 查看资源使用
docker system df
```

## 🆘 紧急恢复

如果所有方法都失败，可以：

1. **完全清理并重新开始**

```bash
# 停止所有容器
docker stop $(docker ps -aq) 2>/dev/null

# 删除所有容器
docker rm $(docker ps -aq) 2>/dev/null

# 清理所有网络
docker network prune -f

# 清理所有镜像（可选，会重新下载）
docker rmi $(docker images -q) 2>/dev/null

# 清理所有 volumes（⚠️ 会删除所有数据）
docker volume prune -f
```

2. **重新配置并部署**

```bash
# 运行修复脚本
./fix-deploy-issue.sh

# 重新部署
./deploy-remote.sh
```

## 📚 相关文档

- [DOCKER_DEPLOY.md](./DOCKER_DEPLOY.md) - Docker 部署完整指南
- [Docker网络问题排查.md](./Docker网络问题排查.md) - 网络问题详细排查
- [UBUNTU_DOCKER_SETUP.md](./UBUNTU_DOCKER_SETUP.md) - Ubuntu 服务器部署指南
- [fix-deploy-issue.sh](./fix-deploy-issue.sh) - 修复脚本

---

## ✅ 快速参考

**遇到网络超时问题？**

```bash
# 一键修复
./fix-deploy-issue.sh

# 然后重新部署
./deploy-remote.sh
```

**部署中断了？**

```bash
# 清理并修复
./fix-deploy-issue.sh

# 重新部署
./deploy-remote.sh
```

---

**记住：遇到问题先运行修复脚本！它会自动处理大部分问题！**

