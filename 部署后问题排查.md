# 部署后问题排查指南

## 🔍 常见问题及解决方案

### 问题1：pandas 模块未找到

**错误信息：**
```
ModuleNotFoundError: No module named 'pandas'
```

**原因：**
- Python 依赖安装失败
- 虚拟环境未正确激活
- 镜像源问题

**解决方案：**

#### 方法一：使用修复脚本（推荐）

```bash
cd ~/GkSystem
chmod +x fix-backend-deps.sh
sudo ./fix-backend-deps.sh
```

#### 方法二：手动修复

```bash
cd ~/GkSystem/backend

# 激活虚拟环境
source venv/bin/activate

# 升级 pip
pip3 install --upgrade pip

# 重新安装依赖（尝试多个镜像源）
pip3 install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple || \
pip3 install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple || \
pip3 install -r requirements.txt

# 验证安装
python3 -c "import pandas; print('pandas OK')"
python3 -c "import numpy; print('numpy OK')"
python3 -c "import flask; print('flask OK')"

# 重启后端服务
sudo systemctl restart canteen-backend
```

---

### 问题2：后端服务未启动

**错误信息：**
```
⚠️  后端服务可能未正常启动，请检查日志
```

**解决方案：**

#### 1. 查看服务状态

```bash
sudo systemctl status canteen-backend
```

#### 2. 查看详细日志

```bash
# 查看最近 50 行日志
sudo journalctl -u canteen-backend -n 50

# 实时查看日志
sudo journalctl -u canteen-backend -f
```

#### 3. 检查常见问题

**问题 A：Python 依赖缺失**
```bash
cd ~/GkSystem/backend
source venv/bin/activate
python3 -c "import flask"
# 如果失败，重新安装依赖
```

**问题 B：数据库连接失败**
```bash
# 检查 MySQL 是否运行
sudo systemctl status mysql

# 测试连接
mysql -u root -p -e "SHOW DATABASES;"
```

**问题 C：端口被占用**
```bash
# 检查端口占用
sudo netstat -tulpn | grep 5000
# 或
sudo ss -tulpn | grep 5000

# 如果被占用，停止占用进程
sudo kill <PID>
```

#### 4. 手动启动测试

```bash
cd ~/GkSystem/backend
source venv/bin/activate
python3 app.py
```

如果手动启动成功，说明代码没问题，可能是 systemd 配置问题。

---

### 问题3：前端访问 500 错误

**错误信息：**
- 浏览器显示 500 Internal Server Error
- Nginx 错误日志显示后端连接失败

**解决方案：**

#### 1. 检查 Nginx 错误日志

```bash
sudo tail -f /var/log/nginx/error.log
```

#### 2. 检查后端是否运行

```bash
# 检查服务状态
sudo systemctl status canteen-backend

# 测试后端 API
curl http://localhost:5000/health
```

#### 3. 检查 Nginx 配置

```bash
# 测试配置
sudo nginx -t

# 查看配置
sudo cat /etc/nginx/sites-available/canteen
```

#### 4. 检查防火墙

```bash
# 检查 UFW 状态
sudo ufw status

# 确保允许本地连接
sudo ufw allow from 127.0.0.1 to any port 5000
```

#### 5. 重启服务

```bash
# 重启后端
sudo systemctl restart canteen-backend

# 重启 Nginx
sudo systemctl restart nginx
```

---

### 问题4：数据预处理失败

**错误信息：**
```
⚠️  数据预处理可能已有数据或遇到错误，继续...
```

**解决方案：**

#### 1. 手动运行数据预处理

```bash
cd ~/GkSystem/backend
source venv/bin/activate

# 运行数据预处理
python3 data/preprocessor.py

# 查看错误信息
```

#### 2. 检查数据库连接

```bash
# 测试数据库连接
mysql -u root -p -e "USE canteen_recommendation; SHOW TABLES;"
```

#### 3. 检查数据是否存在

```bash
mysql -u root -p -e "USE canteen_recommendation; SELECT COUNT(*) FROM dishes;"
```

如果数据已存在，预处理可能会跳过，这是正常的。

---

### 问题5：模型训练失败

**错误信息：**
```
⚠️  模型训练可能遇到错误，继续...
```

**解决方案：**

#### 1. 手动运行模型训练

```bash
cd ~/GkSystem/backend
source venv/bin/activate

# 运行模型训练
python3 train_model.py

# 查看详细错误
```

#### 2. 检查数据是否足够

```bash
# 检查评分数据
mysql -u root -p -e "USE canteen_recommendation; SELECT COUNT(*) FROM ratings;"
```

如果评分数据太少（< 10 条），模型训练可能会失败，这是正常的。

#### 3. 检查模型目录权限

```bash
cd ~/GkSystem/backend
ls -la models/
# 确保有写入权限
chmod 755 models/
```

---

## 🔧 快速修复命令

### 完整修复流程

```bash
# 1. 修复后端依赖
cd ~/GkSystem
sudo ./fix-backend-deps.sh

# 2. 检查后端服务
sudo systemctl status canteen-backend

# 3. 如果服务未运行，重启
sudo systemctl restart canteen-backend

# 4. 检查后端 API
curl http://localhost:5000/health

# 5. 检查 Nginx
sudo systemctl restart nginx

# 6. 测试前端
curl http://localhost/
```

---

## 📋 检查清单

### 后端检查

- [ ] Python 虚拟环境存在
- [ ] 所有依赖已安装（pandas, numpy, flask 等）
- [ ] 后端服务运行中
- [ ] 后端 API 可访问（`curl http://localhost:5000/health`）
- [ ] 数据库连接正常
- [ ] Redis 连接正常

### 前端检查

- [ ] 前端已构建（`dist` 目录存在）
- [ ] Nginx 运行中
- [ ] Nginx 配置正确
- [ ] 前端文件可访问（`curl http://localhost/`）

### 网络检查

- [ ] 防火墙已配置
- [ ] 端口已开放（80, 5000）
- [ ] 公网 IP 可访问

---

## 📚 相关文档

- [fix-backend-deps.sh](./fix-backend-deps.sh) - 后端依赖修复脚本
- [Ubuntu20.04部署说明.md](./Ubuntu20.04部署说明.md) - 部署说明
- [云主机非Docker部署指南.md](./云主机非Docker部署指南.md) - 完整部署指南

---

**如果问题仍然存在，请提供详细的错误日志！**

