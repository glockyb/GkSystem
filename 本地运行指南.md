# 本地运行指南（无需 Docker）

如果无法访问 Docker 官网，可以使用本地开发方式运行项目。这种方式不需要 Docker，直接在本地安装所需服务。

## 第一步：安装必要软件

### 1. 安装 Python 3

```bash
# 使用 Homebrew 安装
brew install python3

# 验证安装
python3 --version
pip3 --version
```

### 2. 安装 MySQL

```bash
# 安装 MySQL
brew install mysql

# 启动 MySQL 服务
brew services start mysql

# 验证安装
mysql --version
```

### 3. 安装 Redis

```bash
# 安装 Redis
brew install redis

# 启动 Redis 服务
brew services start redis

# 验证安装
redis-cli --version
```

### 4. Node.js（已安装 ✓）

你已经安装了 Node.js 16.9.1，无需再安装。

## 第二步：配置数据库

### 1. 创建数据库

```bash
# 登录 MySQL（初始密码通常为空，或查看 Homebrew 安装时的提示）
mysql -u root -p

# 在 MySQL 中执行（如果密码为空，直接按回车）
# 创建数据库
CREATE DATABASE canteen_recommendation CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 退出 MySQL
exit;
```

### 2. 导入初始化脚本

```bash
cd /private/tmp/canteen-recommendation-system
mysql -u root -p canteen_recommendation < backend/database/init.sql
```

## 第三步：配置后端

### 1. 安装 Python 依赖

```bash
cd /private/tmp/canteen-recommendation-system/backend

# 创建虚拟环境（推荐）
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip3 install -r requirements.txt
```

### 2. 配置数据库连接

检查 `backend/config.py` 中的数据库配置，默认配置为：
- MySQL 主机: localhost
- MySQL 端口: 3306
- MySQL 用户: root
- MySQL 密码: password（如果不同，需要修改）

如果需要修改，编辑 `backend/config.py` 或创建 `.env` 文件。

### 3. 初始化数据和训练模型

```bash
cd /private/tmp/canteen-recommendation-system/backend

# 确保虚拟环境已激活
source venv/bin/activate

# 数据预处理
python3 data/preprocessor.py

# 训练推荐模型
python3 train_model.py
```

### 4. 启动后端服务

```bash
# 确保虚拟环境已激活
source venv/bin/activate

# 启动后端
python3 app.py
```

后端将在 http://localhost:5000 启动

## 第四步：配置前端

### 1. 安装前端依赖

```bash
cd /private/tmp/canteen-recommendation-system/frontend
npm install
```

### 2. 启动前端开发服务器

```bash
npm run dev
```

前端将在 http://localhost:5173 启动（Vite 默认端口）

## 第五步：访问系统

- **前端界面**: http://localhost:5173
- **后端 API**: http://localhost:5000
- **健康检查**: http://localhost:5000/health

## 常用命令

### 后端

```bash
cd /private/tmp/canteen-recommendation-system/backend
source venv/bin/activate  # 激活虚拟环境
python3 app.py            # 启动后端
```

### 前端

```bash
cd /private/tmp/canteen-recommendation-system/frontend
npm run dev    # 启动开发服务器
npm run build  # 构建生产版本
```

### 数据库

```bash
# 启动 MySQL
brew services start mysql

# 停止 MySQL
brew services stop mysql

# 启动 Redis
brew services start redis

# 停止 Redis
brew services stop redis

# 查看服务状态
brew services list
```

## 故障排查

### MySQL 连接失败

```bash
# 检查 MySQL 是否运行
brew services list

# 检查 MySQL 密码
# 如果忘记密码，可能需要重置
mysql -u root -p

# 如果连接失败，检查 config.py 中的配置
```

### Redis 连接失败

```bash
# 检查 Redis 是否运行
brew services list
redis-cli ping  # 应该返回 PONG

# Redis 连接失败不影响主要功能，只是推荐功能可能受影响
```

### Python 依赖安装失败

```bash
# 使用国内镜像源
pip3 install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或使用阿里云镜像
pip3 install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
```

### 前端依赖安装失败

```bash
# 使用国内镜像源
npm install --registry=https://registry.npmmirror.com

# 或使用淘宝镜像
npm install --registry=https://registry.npm.taobao.org
```

## 优势

✅ 不需要 Docker，无需访问 Docker 官网  
✅ 本地开发更灵活，便于调试  
✅ 性能更好，无容器开销  
✅ 可以直接修改代码，实时生效  

## 注意事项

1. 确保 MySQL 和 Redis 服务已启动
2. 首次运行需要先初始化数据和训练模型
3. 修改代码后需要重启服务才能生效
4. 生产环境建议使用 Docker 部署

祝使用愉快！🎉

