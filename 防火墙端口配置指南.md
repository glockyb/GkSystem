# 防火墙端口配置指南

## 📋 端口列表总览

本项目中涉及以下端口，根据部署方式和安全需求，需要选择性开放。

### 端口分类

| 端口 | 服务 | 必须开放 | 说明 |
|------|------|---------|------|
| **22** | SSH | ✅ 是 | SSH 远程连接（必需） |
| **8080** | 前端 (Nginx) | ✅ 是 | 用户访问前端界面（必需） |
| **5000** | 后端 (Flask API) | ⚠️ 可选 | 后端 API（如果使用 Nginx 代理，可不开放） |
| **3306** | MySQL | ❌ 否 | 数据库（仅内部使用，不建议开放） |
| **6379** | Redis | ❌ 否 | 缓存（仅内部使用，不建议开放） |
| **443** | HTTPS | ✅ 是 | 如果配置 HTTPS（可选） |
| **80** | HTTP | ⚠️ 可选 | 如果使用 Nginx 反向代理（可选） |

---

## 🔒 推荐配置方案

### 方案一：最小化开放（推荐 - 生产环境）

**只开放必要的端口：**

```bash
# Ubuntu/Debian (UFW)
sudo ufw allow 22/tcp    # SSH（必需）
sudo ufw allow 8080/tcp  # 前端访问（必需）
sudo ufw enable
```

**说明：**
- ✅ 前端通过 8080 端口访问
- ✅ 前端内置 Nginx 反向代理，自动转发 API 请求到后端
- ❌ MySQL 和 Redis 不对外开放（容器内部通信）
- ❌ 后端 API 不对外开放（通过前端 Nginx 代理）

### 方案二：标准配置（开发/测试环境）

**开放前端和后端端口：**

```bash
sudo ufw allow 22/tcp    # SSH（必需）
sudo ufw allow 8080/tcp  # 前端访问（必需）
sudo ufw allow 5000/tcp  # 后端 API（开发/测试）
sudo ufw enable
```

**说明：**
- ✅ 前端和后端都可以直接访问
- ✅ 方便调试和测试
- ⚠️ 适合开发环境

### 方案三：完全开放（不推荐 - 仅内网）

**开放所有端口：**

```bash
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 8080/tcp  # 前端
sudo ufw allow 5000/tcp  # 后端
sudo ufw allow 3306/tcp  # MySQL（不推荐）
sudo ufw allow 6379/tcp  # Redis（不推荐）
sudo ufw enable
```

**⚠️ 警告：**
- ❌ MySQL 和 Redis 不应该对外开放（安全风险）
- ❌ 仅在完全隔离的内网环境中使用

---

## 🔧 详细配置说明

### 1. SSH 端口 (22)

**必须开放** - 用于远程连接服务器

```bash
# Ubuntu/Debian
sudo ufw allow 22/tcp

# CentOS/RHEL
sudo firewall-cmd --permanent --add-service=ssh
sudo firewall-cmd --reload
```

**说明：**
- 这是连接服务器的唯一方式
- 如果修改了 SSH 端口，需要开放新端口

---

### 2. 前端端口 (8080)

**必须开放** - 用户访问前端界面

```bash
# Ubuntu/Debian
sudo ufw allow 8080/tcp

# CentOS/RHEL
sudo firewall-cmd --permanent --add-port=8080/tcp
sudo firewall-cmd --reload
```

**访问地址：**
- 本地：`http://localhost:8080`
- 服务器：`http://your-server-ip:8080`

**说明：**
- 前端使用 Nginx，内部自动代理 `/api/*` 到后端
- 用户只需要访问 8080 端口即可

---

### 3. 后端 API 端口 (5000)

**可选开放** - 后端 Flask API 服务

```bash
# Ubuntu/Debian
sudo ufw allow 5000/tcp

# CentOS/RHEL
sudo firewall-cmd --permanent --add-port=5000/tcp
sudo firewall-cmd --reload
```

**访问地址：**
- 本地：`http://localhost:5000`
- API：`http://your-server-ip:5000/api/v1/*`
- 健康检查：`http://your-server-ip:5000/health`

**说明：**
- 如果使用前端 Nginx 代理（推荐），**不需要开放**此端口
- 前端会自动将 `/api/*` 请求代理到后端
- 如果需要直接调用 API 或调试，可以开放

---

### 4. MySQL 端口 (3306)

**❌ 不建议开放** - 数据库服务

**原因：**
- 安全风险：暴露数据库到公网
- 不需要：前端和后端通过 Docker 内部网络访问

**仅在以下情况下考虑：**
- 完全隔离的内网环境
- 需要远程数据库管理工具连接

```bash
# 如果必须开放（不推荐）
sudo ufw allow from YOUR_IP to any port 3306  # 只允许特定 IP
# 或
sudo ufw allow 3306/tcp  # 允许所有 IP（非常不推荐）
```

**更好的做法：**
- 使用 SSH 隧道连接数据库
- 使用 Docker exec 进入容器管理数据库

---

### 5. Redis 端口 (6379)

**❌ 不建议开放** - 缓存服务

**原因：**
- 安全风险：Redis 暴露到公网可能被攻击
- 不需要：应用通过 Docker 内部网络访问

**仅在以下情况下考虑：**
- 完全隔离的内网环境
- 需要远程 Redis 客户端连接

```bash
# 如果必须开放（不推荐）
sudo ufw allow from YOUR_IP to any port 6379  # 只允许特定 IP
```

---

### 6. HTTPS 端口 (443)

**可选开放** - 如果配置 SSL 证书

```bash
# Ubuntu/Debian
sudo ufw allow 443/tcp

# CentOS/RHEL
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

**说明：**
- 如果使用 Let's Encrypt 等 SSL 证书
- 配合 Nginx 反向代理使用

---

### 7. HTTP 端口 (80)

**可选开放** - 如果使用外部 Nginx 反向代理

```bash
# Ubuntu/Debian
sudo ufw allow 80/tcp

# CentOS/RHEL
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --reload
```

**说明：**
- 如果使用外部 Nginx 反向代理（非 Docker 内的 Nginx）
- 通常配置为自动重定向到 HTTPS

---

## 🛡️ 防火墙配置命令

### Ubuntu/Debian (UFW)

```bash
# 查看当前状态
sudo ufw status

# 启用防火墙
sudo ufw enable

# 禁用防火墙
sudo ufw disable

# 允许端口
sudo ufw allow 8080/tcp

# 允许特定 IP 访问端口
sudo ufw allow from 192.168.1.100 to any port 5000

# 删除规则
sudo ufw delete allow 5000/tcp

# 查看规则编号
sudo ufw status numbered

# 删除指定编号的规则
sudo ufw delete 1
```

### CentOS/RHEL (firewalld)

```bash
# 查看当前状态
sudo firewall-cmd --list-all

# 允许端口
sudo firewall-cmd --permanent --add-port=8080/tcp

# 允许服务
sudo firewall-cmd --permanent --add-service=http

# 重新加载配置
sudo firewall-cmd --reload

# 删除规则
sudo firewall-cmd --permanent --remove-port=5000/tcp
sudo firewall-cmd --reload
```

### 云服务器安全组

如果使用阿里云、腾讯云、AWS 等云服务器，还需要在控制台配置安全组规则：

**阿里云/腾讯云：**
1. 登录云服务器控制台
2. 找到安全组配置
3. 添加入站规则：
   - 端口 22，协议 TCP，允许
   - 端口 8080，协议 TCP，允许
   - 端口 5000，协议 TCP，允许（可选）

**AWS：**
1. EC2 控制台
2. Security Groups
3. Edit Inbound Rules：
   - Port 22 (SSH)
   - Port 8080 (Custom TCP)
   - Port 5000 (Custom TCP) - 可选

---

## 📊 部署场景端口需求

### 场景一：生产环境（推荐配置）

**开放端口：**
- ✅ 22 (SSH)
- ✅ 8080 (前端)

**不开放：**
- ❌ 5000 (通过前端 Nginx 代理)
- ❌ 3306 (内部访问)
- ❌ 6379 (内部访问)

**配置命令：**
```bash
sudo ufw allow 22/tcp
sudo ufw allow 8080/tcp
sudo ufw enable
```

---

### 场景二：开发/测试环境

**开放端口：**
- ✅ 22 (SSH)
- ✅ 8080 (前端)
- ✅ 5000 (后端 API)

**不开放：**
- ❌ 3306 (内部访问)
- ❌ 6379 (内部访问)

**配置命令：**
```bash
sudo ufw allow 22/tcp
sudo ufw allow 8080/tcp
sudo ufw allow 5000/tcp
sudo ufw enable
```

---

### 场景三：使用外部 Nginx 反向代理

**开放端口：**
- ✅ 22 (SSH)
- ✅ 80 (HTTP - Nginx)
- ✅ 443 (HTTPS - Nginx)

**不开放：**
- ❌ 8080 (不直接访问)
- ❌ 5000 (通过 Nginx 代理)
- ❌ 3306 (内部访问)
- ❌ 6379 (内部访问)

**配置命令：**
```bash
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
```

---

## 🔍 检查端口占用和状态

### 检查端口是否被监听

```bash
# 查看所有监听的端口
sudo netstat -tulpn | grep LISTEN

# 或使用 ss 命令
sudo ss -tulpn | grep LISTEN

# 检查特定端口
sudo netstat -tulpn | grep :8080
sudo ss -tulpn | grep :8080
```

### 检查端口是否开放

```bash
# 从服务器本地测试
curl -I http://localhost:8080

# 从外部测试（需要另一台机器）
curl -I http://your-server-ip:8080

# 测试端口是否可达
telnet your-server-ip 8080
```

### 检查防火墙状态

```bash
# Ubuntu/Debian
sudo ufw status verbose

# CentOS/RHEL
sudo firewall-cmd --list-all

# 查看防火墙日志
sudo tail -f /var/log/ufw.log  # Ubuntu/Debian
sudo journalctl -u firewalld -f  # CentOS/RHEL
```

---

## 🔐 安全建议

### 1. 最小化开放原则

- ✅ 只开放必要的端口
- ❌ 不要开放数据库端口到公网
- ❌ 不要开放 Redis 端口到公网

### 2. 限制 IP 访问（如果可能）

```bash
# 只允许特定 IP 访问后端 API
sudo ufw allow from 192.168.1.0/24 to any port 5000

# 只允许特定 IP 访问 SSH
sudo ufw allow from YOUR_IP to any port 22
```

### 3. 修改 SSH 端口（可选）

```bash
# 编辑 SSH 配置
sudo nano /etc/ssh/sshd_config

# 修改端口（例如改为 2222）
Port 2222

# 重启 SSH
sudo systemctl restart sshd

# 开放新端口
sudo ufw allow 2222/tcp
```

### 4. 使用 Fail2Ban（防止暴力破解）

```bash
# 安装 Fail2Ban
sudo apt install fail2ban  # Ubuntu/Debian
sudo yum install fail2ban   # CentOS/RHEL

# 启动服务
sudo systemctl enable fail2ban
sudo systemctl start fail2ban
```

### 5. 定期更新和监控

```bash
# 定期更新系统
sudo apt update && sudo apt upgrade  # Ubuntu/Debian
sudo yum update  # CentOS/RHEL

# 监控端口访问
sudo netstat -ant | awk '{print $6}' | sort | uniq -c | sort -n
```

---

## 📝 快速配置脚本

创建防火墙配置脚本 `configure-firewall.sh`：

```bash
#!/bin/bash
# 防火墙配置脚本

echo "配置防火墙规则..."

# 检查 UFW 是否安装
if command -v ufw &> /dev/null; then
    echo "使用 UFW 配置防火墙..."
    
    # 重置规则（可选，谨慎使用）
    # sudo ufw --force reset
    
    # 允许 SSH
    sudo ufw allow 22/tcp
    echo "✅ 允许 SSH (22)"
    
    # 允许前端
    sudo ufw allow 8080/tcp
    echo "✅ 允许前端 (8080)"
    
    # 询问是否允许后端 API
    read -p "是否允许后端 API 端口 5000? (y/n) " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        sudo ufw allow 5000/tcp
        echo "✅ 允许后端 API (5000)"
    fi
    
    # 启用防火墙
    sudo ufw enable
    
    # 显示状态
    sudo ufw status verbose
    
elif command -v firewall-cmd &> /dev/null; then
    echo "使用 firewalld 配置防火墙..."
    
    # 允许 SSH
    sudo firewall-cmd --permanent --add-service=ssh
    echo "✅ 允许 SSH (22)"
    
    # 允许前端
    sudo firewall-cmd --permanent --add-port=8080/tcp
    echo "✅ 允许前端 (8080)"
    
    # 询问是否允许后端 API
    read -p "是否允许后端 API 端口 5000? (y/n) " -n 1 -r
    echo
    if [[ $REPLY =~ ^[Yy]$ ]]; then
        sudo firewall-cmd --permanent --add-port=5000/tcp
        echo "✅ 允许后端 API (5000)"
    fi
    
    # 重新加载配置
    sudo firewall-cmd --reload
    
    # 显示状态
    sudo firewall-cmd --list-all
    
else
    echo "❌ 未找到防火墙管理工具（ufw 或 firewalld）"
    exit 1
fi

echo ""
echo "✅ 防火墙配置完成！"
```

使用：
```bash
chmod +x configure-firewall.sh
./configure-firewall.sh
```

---

## 📚 相关文档

- [DOCKER_DEPLOY.md](./DOCKER_DEPLOY.md) - Docker 部署指南
- [UBUNTU_DOCKER_SETUP.md](./UBUNTU_DOCKER_SETUP.md) - Ubuntu 服务器部署指南
- [部署故障处理指南.md](./部署故障处理指南.md) - 部署故障处理

---

## ✅ 总结

### 必需开放的端口

1. **22 (SSH)** - 远程连接服务器

### 推荐开放的端口

2. **8080 (前端)** - 用户访问前端界面

### 可选开放的端口

3. **5000 (后端)** - 如果需要直接访问 API 或调试

### 不建议开放的端口

4. **3306 (MySQL)** - 安全风险，使用内部网络
5. **6379 (Redis)** - 安全风险，使用内部网络

### 快速配置命令（生产环境推荐）

```bash
sudo ufw allow 22/tcp    # SSH（必需）
sudo ufw allow 8080/tcp  # 前端（必需）
sudo ufw enable
```

---

**记住：最小化开放原则，只开放必要的端口！**

