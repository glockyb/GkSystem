# 服务管理指南

## 📋 部署流程说明

### 首次部署（只需一次）

```bash
# 运行部署脚本（只需运行一次）
cd ~/gksys/GkSystem
sudo ./deploy-cloud-server.sh
```

**部署脚本会：**
- ✅ 安装系统依赖
- ✅ 配置数据库和服务
- ✅ 部署前后端代码
- ✅ 创建 systemd 服务
- ✅ 配置 Nginx
- ✅ 设置开机自启

**部署完成后，所有服务都会自动启动并设置为开机自启。**

---

## 🔧 服务管理（日常使用）

### 后端服务管理

```bash
# 启动服务
sudo systemctl start canteen-backend

# 停止服务
sudo systemctl stop canteen-backend

# 重启服务
sudo systemctl restart canteen-backend

# 查看状态
sudo systemctl status canteen-backend

# 查看日志
sudo journalctl -u canteen-backend -f

# 查看最近日志
sudo journalctl -u canteen-backend -n 50

# 设置开机自启（通常已设置）
sudo systemctl enable canteen-backend

# 取消开机自启
sudo systemctl disable canteen-backend
```

### Nginx 服务管理

```bash
# 启动
sudo systemctl start nginx

# 停止
sudo systemctl stop nginx

# 重启
sudo systemctl restart nginx

# 重新加载配置（不中断服务）
sudo systemctl reload nginx

# 查看状态
sudo systemctl status nginx

# 查看日志
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/canteen-access.log
```

### MySQL 服务管理

```bash
# 启动
sudo systemctl start mysql

# 停止
sudo systemctl stop mysql

# 重启
sudo systemctl restart mysql

# 查看状态
sudo systemctl status mysql
```

### Redis 服务管理

```bash
# 启动
sudo systemctl start redis-server

# 停止
sudo systemctl stop redis-server

# 重启
sudo systemctl restart redis-server

# 查看状态
sudo systemctl status redis-server
```

---

## 🚀 常用操作场景

### 场景1：重启所有服务

```bash
# 重启所有服务
sudo systemctl restart canteen-backend
sudo systemctl restart nginx
sudo systemctl restart mysql
sudo systemctl restart redis-server

# 或使用脚本
cat > ~/restart-all.sh <<'EOF'
#!/bin/bash
sudo systemctl restart canteen-backend
sudo systemctl restart nginx
sudo systemctl restart mysql
sudo systemctl restart redis-server
echo "所有服务已重启"
EOF
chmod +x ~/restart-all.sh
```

### 场景2：查看所有服务状态

```bash
# 查看所有相关服务状态
sudo systemctl status canteen-backend nginx mysql redis-server

# 或逐个查看
for service in canteen-backend nginx mysql redis-server; do
    echo "=== $service ==="
    sudo systemctl is-active $service
done
```

### 场景3：停止所有服务

```bash
sudo systemctl stop canteen-backend
sudo systemctl stop nginx
# MySQL 和 Redis 通常不需要停止
```

### 场景4：启动所有服务

```bash
sudo systemctl start mysql
sudo systemctl start redis-server
sudo systemctl start canteen-backend
sudo systemctl start nginx
```

---

## 🔄 更新和维护

### 更新代码

```bash
cd ~/gksys/GkSystem

# 拉取最新代码（如果使用 Git）
git pull

# 更新后端
cd backend
source venv/bin/activate
pip3 install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
sudo systemctl restart canteen-backend

# 更新前端
cd ../frontend
npm install
npm run build
sudo systemctl reload nginx
```

### 更新配置

```bash
# 修改 Nginx 配置后
sudo nano /etc/nginx/sites-available/canteen
sudo nginx -t  # 测试配置
sudo systemctl reload nginx  # 重新加载

# 修改后端配置后
sudo nano ~/gksys/GkSystem/backend/.env
sudo systemctl restart canteen-backend
```

---

## 📊 服务状态检查

### 快速检查脚本

```bash
cat > ~/check-services.sh <<'EOF'
#!/bin/bash
echo "=== 服务状态检查 ==="
for service in canteen-backend nginx mysql redis-server; do
    if sudo systemctl is-active $service >/dev/null 2>&1; then
        echo "✅ $service: 运行中"
    else
        echo "❌ $service: 未运行"
    fi
done
echo ""
echo "=== 端口检查 ==="
netstat -tuln | grep -E ':80|:5000|:3306|:6379' || ss -tuln | grep -E ':80|:5000|:3306|:6379'
EOF
chmod +x ~/check-services.sh
```

---

## 🔍 故障排查

### 服务无法启动

```bash
# 查看服务状态
sudo systemctl status canteen-backend

# 查看详细日志
sudo journalctl -u canteen-backend -n 50

# 手动测试启动
cd ~/gksys/GkSystem/backend
source venv/bin/activate
python3 app.py
```

### 服务频繁重启

```bash
# 查看重启次数
sudo systemctl status canteen-backend | grep "restart"

# 查看失败原因
sudo journalctl -u canteen-backend --since "10 minutes ago"
```

---

## 📝 服务配置文件位置

### systemd 服务文件

```bash
# 后端服务配置
/etc/systemd/system/canteen-backend.service

# 查看配置
sudo cat /etc/systemd/system/canteen-backend.service

# 修改配置后需要重新加载
sudo systemctl daemon-reload
sudo systemctl restart canteen-backend
```

### Nginx 配置

```bash
# 配置文件
/etc/nginx/sites-available/canteen

# 查看配置
sudo cat /etc/nginx/sites-available/canteen

# 测试配置
sudo nginx -t
```

---

## ✅ 总结

### 部署阶段（只需一次）

- ✅ 运行 `deploy-cloud-server.sh` 完成初始部署
- ✅ 所有服务自动启动并设置开机自启

### 日常使用阶段

- ✅ 使用 `systemctl` 命令管理服务
- ✅ 服务会自动开机启动
- ✅ 无需再次运行部署脚本

### 更新维护阶段

- ✅ 更新代码后重启相应服务
- ✅ 修改配置后重新加载服务
- ✅ 查看日志排查问题

---

**记住：部署脚本只需运行一次，后续通过 systemctl 管理服务！**

