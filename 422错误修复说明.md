# 422 错误修复说明

## 问题描述
前端请求返回 422 (UNPROCESSABLE ENTITY) 错误，主要影响：
- `/api/v1/recommendations` - 推荐功能
- `/api/v1/ratings/*` - 评分功能

## 修复内容

### 1. 前端增强 (`frontend/src/api/index.js`)

#### 请求拦截器增强
- ✅ 自动清理 token 格式（去除空格）
- ✅ 添加 `X-Request-ID` 请求头用于追踪
- ✅ 自动设置 `Content-Type` 和 `Accept` 头
- ✅ 开发环境日志记录

#### 响应拦截器增强
- ✅ 处理 401 错误（token 过期/无效）
- ✅ **新增：处理 422 错误（JWT token 格式错误）**
- ✅ 自动清除无效 token 并跳转登录页
- ✅ 详细的错误日志记录

### 2. 后端增强 (`backend/app.py`)

#### JWT 错误处理增强
- ✅ **`expired_token_loader`**: 返回详细错误信息，包含 `error_code`
- ✅ **`invalid_token_loader`**: 
  - 记录完整的请求信息（URL、Method、Headers）
  - 检查 Authorization header 格式
  - 返回详细的错误信息和 `request_id`
- ✅ **`unauthorized_loader`**: 返回详细的错误信息

#### 请求日志记录
- ✅ 添加 `@app.before_request` 钩子
- ✅ 记录所有 API 请求的详细信息
- ✅ 记录 Authorization header（部分显示，保护隐私）

### 3. API 参数验证增强

#### 推荐 API (`backend/api/recommendations.py`)
- ✅ 验证 `user_id` 类型和范围
- ✅ 验证参数 `n` 的范围（1-100）
- ✅ 添加详细的错误日志
- ✅ 返回 `request_id` 用于追踪

#### 评分 API (`backend/api/ratings.py`)
- ✅ 验证 `user_id` 类型和范围
- ✅ 验证 `dish_id` 类型和范围
- ✅ 验证 `rating` 类型和范围（1-5）
- ✅ 验证 `comment` 长度（最大 500 字符）
- ✅ 验证请求体是否存在
- ✅ 添加详细的错误日志和错误代码

## 新增功能

### 1. 请求追踪
- 每个请求自动生成 `X-Request-ID`
- 所有错误响应包含 `request_id`
- 便于前后端日志关联

### 2. 错误代码
所有错误响应现在包含 `error_code`：
- `TOKEN_EXPIRED` - Token 已过期
- `INVALID_TOKEN` - Token 无效
- `MISSING_TOKEN` - 缺少 Token
- `INVALID_USER_ID` - 用户 ID 格式错误
- `INVALID_DISH_ID` - 菜品 ID 格式错误
- `INVALID_RATING_RANGE` - 评分范围错误
- `VALIDATION_ERROR` - 参数验证失败
- `DATABASE_ERROR` - 数据库错误

### 3. 详细日志
- 后端记录所有 JWT 相关错误
- 记录请求头、URL、方法等信息
- 便于问题排查

## 使用说明

### 前端
1. **自动处理**：前端会自动处理 422 错误
2. **清除 token**：如果检测到 token 无效，会自动清除并跳转登录
3. **错误提示**：用户会看到友好的错误提示

### 后端
1. **查看日志**：
   ```bash
   sudo journalctl -u canteen-backend -f
   ```
2. **搜索特定请求**：
   ```bash
   sudo journalctl -u canteen-backend | grep "Request ID: xxx"
   ```

## 测试建议

1. **清除浏览器缓存和 localStorage**
2. **重新登录获取新 token**
3. **测试推荐功能**
4. **测试评分功能**
5. **查看后端日志确认请求正常**

## 常见问题

### Q: 仍然出现 422 错误？
A: 
1. 清除浏览器 localStorage 中的所有数据
2. 重新登录
3. 检查后端日志查看具体错误信息

### Q: 如何查看请求 ID？
A: 
- 浏览器开发者工具 → Network → 查看请求头中的 `X-Request-ID`
- 后端日志中搜索该 ID 查看完整请求信息

### Q: Token 格式是什么？
A: 
- 正确格式：`Authorization: Bearer <token>`
- 前端会自动添加 `Bearer` 前缀

## 部署步骤

1. **更新代码**：
   ```bash
   cd ~/gksys/GkSystem
   git pull  # 或手动更新文件
   ```

2. **重新构建前端**：
   ```bash
   cd frontend
   npm run build
   ```

3. **部署前端**：
   ```bash
   sudo cp -r dist/* /var/www/canteen/
   ```

4. **重启后端**：
   ```bash
   sudo systemctl restart canteen-backend
   ```

5. **验证**：
   ```bash
   sudo systemctl status canteen-backend
   curl http://localhost:5000/health
   ```

## 注意事项

1. **生产环境**：建议关闭详细的日志记录（修改 `import.meta.env.DEV` 检查）
2. **安全性**：确保 JWT_SECRET_KEY 在生产环境中使用强密钥
3. **监控**：建议监控 422 错误率，及时发现认证问题

