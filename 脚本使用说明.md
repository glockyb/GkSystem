# 启动脚本使用说明

本项目提供了多个启动脚本，用于不同的使用场景。本文档说明各个脚本的用途和使用方法。

## 📋 脚本列表

### 本地开发脚本

#### 1. `启动前端.sh` - 前端开发服务器
**用途**: 启动前端开发服务器（Vue 3 + Vite）  
**位置**: 项目根目录  
**使用场景**: 本地开发前端代码

```bash
# 在项目根目录运行
./启动前端.sh

# 或
bash 启动前端.sh
```

**功能**:
- ✅ 自动检查 Node.js 环境
- ✅ 自动安装/更新依赖
- ✅ 检测 npm 脚本配置
- ✅ 启动开发服务器（端口 8080）

**访问地址**: http://localhost:8080

---

#### 2. `启动后端.sh` - 后端开发服务器
**用途**: 启动后端 Flask 服务  
**位置**: 项目根目录  
**使用场景**: 本地开发后端代码

```bash
# 在项目根目录运行
./启动后端.sh

# 或
bash 启动后端.sh
```

**功能**:
- ✅ 跨平台支持（macOS / Linux）
- ✅ 自动检查 MySQL 和 Redis 服务
- ✅ 自动创建和初始化数据库
- ✅ 自动检查 Python 虚拟环境
- ✅ 自动安装 Python 依赖
- ✅ 检查并训练推荐模型
- ✅ 启动 Flask 服务（端口 5000）

**访问地址**: http://localhost:5000

---

#### 3. `setup-local.sh` - 本地环境设置脚本（macOS）
**用途**: 一键设置本地开发环境  
**位置**: 项目根目录  
**使用场景**: 首次在 macOS 上设置开发环境

```bash
# 在项目根目录运行
./setup-local.sh

# 或
bash setup-local.sh
```

**功能**:
- ✅ 检查并安装 Homebrew
- ✅ 安装 Python 3、MySQL、Redis、Node.js
- ✅ 启动 MySQL 和 Redis 服务
- ✅ 创建数据库
- ✅ 导入数据库初始化脚本
- ✅ 创建 Python 虚拟环境
- ✅ 安装 Python 和前端依赖

**注意**: 此脚本专为 macOS 设计，使用 Homebrew 安装软件。

---

#### 4. `start-local.sh` - 本地服务启动脚本（macOS）
**用途**: 在 macOS 上快速启动后端服务  
**位置**: 项目根目录  
**使用场景**: macOS 上快速启动后端（假设前端单独启动）

```bash
# 在项目根目录运行
./start-local.sh

# 或
bash start-local.sh
```

**功能**:
- ✅ 检查并启动 MySQL 和 Redis 服务（使用 brew services）
- ✅ 检查虚拟环境
- ✅ 激活虚拟环境并安装依赖
- ✅ 启动 Flask 服务

---

#### 5. `backend/start.sh` - 后端专用启动脚本
**用途**: 从 backend 目录直接启动后端服务  
**位置**: `backend/` 目录  
**使用场景**: 在 backend 目录内直接启动服务

```bash
# 在 backend 目录运行
cd backend
./start.sh

# 或
bash start.sh
```

**功能**:
- ✅ 检查 Python 环境
- ✅ 创建/使用虚拟环境
- ✅ 安装 Python 依赖
- ✅ 检查数据库和 Redis 连接
- ✅ 检查并训练推荐模型
- ✅ 启动 Flask 服务

---

### Docker 脚本

#### 6. `start-docker.sh` - Docker Compose 启动脚本
**用途**: 使用 Docker Compose 启动所有服务  
**位置**: 项目根目录  
**使用场景**: 使用 Docker 运行整个系统

```bash
# 在项目根目录运行
./start-docker.sh

# 或
bash start-docker.sh
```

**功能**:
- ✅ 检查 Docker 环境
- ✅ 支持 Docker Compose V1 和 V2
- ✅ 构建并启动所有容器
- ✅ 等待 MySQL 和 Redis 健康检查
- ✅ 初始化数据和训练模型
- ✅ 显示访问地址和常用命令

**访问地址**:
- 前端: http://localhost:8080
- 后端: http://localhost:5000

---

#### 7. `deploy-remote.sh` - 远程服务器部署脚本
**用途**: 在远程服务器上部署系统  
**位置**: 项目根目录  
**使用场景**: 在远程 Ubuntu 服务器上部署

```bash
# 在远程服务器上运行
./deploy-remote.sh

# 或
bash deploy-remote.sh
```

**功能**:
- ✅ 检查 Docker 环境
- ✅ 配置 API 地址
- ✅ 构建并启动所有服务
- ✅ 初始化数据和模型

详细说明请查看 [UBUNTU_DOCKER_SETUP.md](./UBUNTU_DOCKER_SETUP.md)

---

## 🚀 快速开始

### 方案一：本地开发（macOS）

```bash
# 1. 首次设置环境
./setup-local.sh

# 2. 启动后端（终端 1）
./启动后端.sh

# 3. 启动前端（终端 2）
./启动前端.sh
```

### 方案二：本地开发（Linux/Ubuntu）

```bash
# 1. 安装依赖（手动或使用系统包管理器）
sudo apt install python3 python3-pip python3-venv mysql-server redis-server nodejs npm

# 2. 启动后端
./启动后端.sh

# 3. 启动前端（新终端）
./启动前端.sh
```

### 方案三：Docker 部署

```bash
# 1. 启动所有服务
./start-docker.sh

# 2. 访问系统
# 前端: http://localhost:8080
# 后端: http://localhost:5000
```

---

## 📝 脚本特性

### 统一的设计

所有脚本都具有以下特性：

1. **颜色输出**: 使用颜色区分信息类型（✅ 成功、⚠️ 警告、❌ 错误）
2. **错误处理**: 使用 `set -e` 在遇到错误时立即退出
3. **日志记录**: 清晰的日志输出，便于调试
4. **自动检查**: 自动检查环境、依赖和服务状态
5. **友好提示**: 提供清晰的错误信息和解决建议

### 跨平台支持

- **macOS**: 使用 Homebrew 管理服务
- **Linux/Ubuntu**: 使用 systemctl 管理服务
- **自动检测**: 脚本会自动检测操作系统类型

---

## 🔧 常见问题

### Q1: 脚本没有执行权限？

```bash
chmod +x 启动前端.sh
chmod +x 启动后端.sh
```

或批量设置权限：
```bash
chmod +x *.sh
chmod +x backend/*.sh
```

### Q2: 后端启动失败，提示 MySQL 连接错误？

**macOS**:
```bash
# 检查 MySQL 服务
brew services list | grep mysql

# 启动 MySQL
brew services start mysql
```

**Linux**:
```bash
# 检查 MySQL 服务
sudo systemctl status mysql

# 启动 MySQL
sudo systemctl start mysql
```

### Q3: 前端启动失败，提示端口被占用？

修改 `frontend/vite.config.js` 中的端口：
```javascript
server: {
  port: 8081,  // 改为其他端口
}
```

### Q4: Docker 脚本提示找不到 docker-compose？

**Docker Compose V2** (推荐):
```bash
docker compose version  # 注意是空格，不是连字符
```

**Docker Compose V1**:
```bash
docker-compose version  # 使用连字符
```

脚本会自动检测并使用正确的命令。

---

## 📚 相关文档

- [README.md](./README.md) - 项目总览
- [本地运行指南.md](./本地运行指南.md) - 详细的本地开发指南
- [DOCKER_DEPLOY.md](./DOCKER_DEPLOY.md) - Docker 部署文档
- [UBUNTU_DOCKER_SETUP.md](./UBUNTU_DOCKER_SETUP.md) - Ubuntu 服务器部署指南

---

## 💡 最佳实践

1. **开发环境**: 使用 `启动前端.sh` 和 `启动后端.sh` 进行本地开发
2. **生产环境**: 使用 Docker 部署（`start-docker.sh` 或 `deploy-remote.sh`）
3. **首次设置**: 使用 `setup-local.sh` 一键配置开发环境
4. **快速测试**: 使用 Docker 快速启动完整环境

---

**如有问题，请查看相应脚本的日志输出或相关文档！**

