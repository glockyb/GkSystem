# 云主机网络问题排查指南

## 📋 问题描述

在云主机上运行部署脚本时，遇到以下错误：
```
Error: failed to resolve reference "docker.io/library/mysql:8.0": 
failed to do request: Head "https://docker.mirrors.ustc.edu.cn/v2/library/mysql/manifests/8.0?ns=docker.io": 
dial tcp: lookup docker.mirrors.ustc.edu.cn on 127.0.0.53:53: no such host

所有镜像仓库都不可用，请检查网络连接
无法连接到 Docker Hub，请检查网络或防火墙设置
```

这是典型的 **DNS 解析失败** 和 **网络连接问题**。

## 🔍 问题原因

### 1. DNS 解析失败

- DNS 服务器（127.0.0.53）无法解析域名
- DNS 服务器配置错误
- DNS 服务未运行

### 2. 云主机网络配置问题

- 云主机安全组未允许出站流量
- 云主机网络配置错误
- 云主机需要代理访问外网

### 3. 防火墙阻止

- 防火墙阻止了 DNS 查询（53端口）
- 防火墙阻止了 HTTPS 连接（443端口）

## ✅ 解决方案

### 方案一：使用网络修复脚本（推荐）

```bash
# 在云主机上运行
sudo ./fix-network-dns.sh
```

脚本会自动：
- ✅ 检查网络连接
- ✅ 诊断 DNS 配置
- ✅ 修复 DNS 配置（使用公共 DNS）
- ✅ 测试镜像仓库连接
- ✅ 配置 Docker 镜像加速器
- ✅ 配置 Docker 代理（如果需要）

### 方案二：手动修复 DNS

#### 1. 检查 DNS 配置

```bash
# 查看当前 DNS 配置
cat /etc/resolv.conf

# 测试 DNS 解析
nslookup docker.io
nslookup docker.mirrors.ustc.edu.cn
```

#### 2. 修复 DNS 配置

```bash
# 备份原配置
sudo cp /etc/resolv.conf /etc/resolv.conf.bak

# 配置公共 DNS
sudo tee /etc/resolv.conf > /dev/null <<EOF
nameserver 8.8.8.8
nameserver 8.8.4.4
nameserver 114.114.114.114
nameserver 223.5.5.5
EOF

# 如果使用 systemd-resolved，可能需要修改
sudo nano /etc/systemd/resolved.conf
# 添加：
# [Resolve]
# DNS=8.8.8.8 8.8.4.4
# FallbackDNS=114.114.114.114 223.5.5.5

# 重启 DNS 服务
sudo systemctl restart systemd-resolved

# 验证 DNS
nslookup docker.io
```

#### 3. 验证网络连接

```bash
# 测试基本网络
ping -c 3 8.8.8.8

# 测试 HTTPS 连接
curl -I https://www.baidu.com
curl -I https://docker.io

# 测试 DNS 解析
nslookup docker.mirrors.ustc.edu.cn
```

### 方案三：检查云主机安全组

#### 阿里云/腾讯云安全组配置

**必需规则（出站）：**
1. 允许 HTTPS（443端口）出站
2. 允许 DNS（53端口）出站
3. 允许 HTTP（80端口）出站（可选）

**配置步骤：**
1. 登录云服务器控制台
2. 找到安全组配置
3. 配置**出站规则**：
   - 协议：TCP
   - 端口：443
   - 目标：0.0.0.0/0
   - 操作：允许

### 方案四：配置云主机代理（如果需要）

某些云主机需要通过代理访问外网。

#### 1. 检查是否需要代理

```bash
# 检查环境变量
env | grep -i proxy

# 测试直连
curl -I https://www.baidu.com
```

#### 2. 配置系统代理

```bash
# 设置代理环境变量
export http_proxy=http://proxy.example.com:8080
export https_proxy=http://proxy.example.com:8080
export no_proxy=localhost,127.0.0.1

# 永久设置（添加到 ~/.bashrc）
echo 'export http_proxy=http://proxy.example.com:8080' >> ~/.bashrc
echo 'export https_proxy=http://proxy.example.com:8080' >> ~/.bashrc
source ~/.bashrc
```

#### 3. 配置 Docker 使用代理

```bash
# 创建 Docker 代理配置目录
sudo mkdir -p /etc/systemd/system/docker.service.d

# 创建代理配置文件
sudo tee /etc/systemd/system/docker.service.d/http-proxy.conf > /dev/null <<EOF
[Service]
Environment="HTTP_PROXY=http://proxy.example.com:8080"
Environment="HTTPS_PROXY=http://proxy.example.com:8080"
Environment="NO_PROXY=localhost,127.0.0.1"
EOF

# 重新加载并重启 Docker
sudo systemctl daemon-reload
sudo systemctl restart docker

# 验证配置
sudo systemctl show --property=Environment docker
```

### 方案五：手动拉取镜像（备用方案）

如果网络问题无法解决，可以手动拉取镜像：

#### 1. 在本地或其他服务器拉取镜像

```bash
# 拉取镜像
docker pull mysql:8.0
docker pull redis:7-alpine
docker pull nginx:alpine
docker pull python:3.9-slim
docker pull node:18-alpine

# 保存镜像为 tar 文件
docker save mysql:8.0 -o mysql-8.0.tar
docker save redis:7-alpine -o redis-7-alpine.tar
docker save nginx:alpine -o nginx-alpine.tar
docker save python:3.9-slim -o python-3.9-slim.tar
docker save node:18-alpine -o node-18-alpine.tar
```

#### 2. 上传镜像到云主机

```bash
# 在本地执行
scp *.tar username@your-server-ip:~/
```

#### 3. 在云主机上加载镜像

```bash
# 在云主机上执行
docker load -i mysql-8.0.tar
docker load -i redis-7-alpine.tar
docker load -i nginx-alpine.tar
docker load -i python-3.9-slim.tar
docker load -i node-18-alpine.tar

# 验证镜像
docker images
```

#### 4. 重新运行部署

```bash
./deploy-remote.sh
```

---

## 🔧 详细故障排查步骤

### 步骤1：检查网络连接

```bash
# 测试基本网络
ping -c 3 8.8.8.8

# 测试 HTTPS 连接
curl -I https://www.baidu.com

# 测试特定域名
curl -I https://docker.io
```

### 步骤2：检查 DNS 配置

```bash
# 查看 DNS 配置
cat /etc/resolv.conf

# 测试 DNS 解析
nslookup docker.io
nslookup docker.mirrors.ustc.edu.cn
dig docker.io
```

### 步骤3：检查防火墙设置

```bash
# 检查 UFW 状态
sudo ufw status

# 检查 firewalld 状态
sudo firewall-cmd --list-all

# 确保允许 DNS 查询
sudo ufw allow out 53/tcp
sudo ufw allow out 53/udp
sudo ufw allow out 443/tcp
```

### 步骤4：检查云主机安全组

**检查项目：**
1. 是否允许 HTTPS（443）出站流量？
2. 是否允许 DNS（53）出站流量？
3. 是否有其他网络限制？

**配置示例（阿里云/腾讯云）：**
- 出站规则：允许所有协议、所有端口（仅出站）
- 或至少允许：TCP 443, UDP 53, TCP 53

### 步骤5：检查云主机路由

```bash
# 查看路由表
ip route

# 测试路由
traceroute 8.8.8.8
traceroute docker.io
```

### 步骤6：检查是否需要代理

```bash
# 检查代理设置
env | grep -i proxy

# 检查 /etc/environment
cat /etc/environment

# 检查系统代理配置
cat /etc/apt/apt.conf.d/*proxy* 2>/dev/null || true
```

---

## 🎯 云主机特定问题

### 问题1：内网 IP + 外网 IP 配置

**情况：** 云主机有内网 IP 和外网 IP

**解决：**
1. 确保外网 IP 已绑定
2. 确保路由配置正确
3. 测试外网连接：
   ```bash
   curl -I https://www.baidu.com
   ```

### 问题2：NAT 网关配置

**情况：** 云主机通过 NAT 网关访问外网

**解决：**
1. 检查 NAT 网关路由表
2. 确保 NAT 网关允许所需流量
3. 检查安全组规则

### 问题3：VPC 网络配置

**情况：** 云主机在 VPC 中，需要配置路由

**解决：**
1. 检查 VPC 路由表
2. 确保有到 Internet 网关的路由
3. 检查网络 ACL 规则

---

## 📝 快速修复命令

### 一键修复（推荐）

```bash
# 运行网络修复脚本
sudo ./fix-network-dns.sh
```

### 手动快速修复

```bash
# 1. 修复 DNS
sudo tee /etc/resolv.conf > /dev/null <<EOF
nameserver 8.8.8.8
nameserver 8.8.4.4
nameserver 114.114.114.114
EOF

# 2. 重启 DNS 服务
sudo systemctl restart systemd-resolved 2>/dev/null || true

# 3. 测试 DNS
nslookup docker.io

# 4. 如果成功，重新运行部署
./deploy-remote.sh
```

---

## 🔍 云主机服务商特定配置

### 阿里云

1. **检查安全组规则**
   - 登录 ECS 控制台
   - 安全组 → 配置规则
   - 出站方向：允许 TCP 443, UDP 53

2. **检查网络配置**
   - 专有网络 VPC → 路由表
   - 确保有到 0.0.0.0/0 的路由

### 腾讯云

1. **检查安全组规则**
   - 云服务器控制台 → 安全组
   - 出站规则：允许 HTTPS, DNS

2. **检查网络配置**
   - 私有网络 → 路由表
   - 确保有默认路由

### AWS

1. **检查安全组规则**
   - EC2 控制台 → Security Groups
   - Outbound rules: Allow HTTPS, DNS

2. **检查路由表**
   - VPC → Route Tables
   - 确保有到 Internet Gateway 的路由

---

## 💡 备用方案

### 方案1：使用代理服务器

如果云主机无法直连外网，配置代理：

```bash
# 配置系统代理
export http_proxy=http://proxy-server:port
export https_proxy=http://proxy-server:port

# 配置 Docker 代理
sudo mkdir -p /etc/systemd/system/docker.service.d
sudo tee /etc/systemd/system/docker.service.d/http-proxy.conf > /dev/null <<EOF
[Service]
Environment="HTTP_PROXY=http://proxy-server:port"
Environment="HTTPS_PROXY=http://proxy-server:port"
EOF

sudo systemctl daemon-reload
sudo systemctl restart docker
```

### 方案2：离线部署镜像

如果无法拉取镜像，手动导入：

```bash
# 在本地或其他网络正常的服务器上
docker pull mysql:8.0
docker save mysql:8.0 | gzip > mysql-8.0.tar.gz

# 上传到云主机
scp mysql-8.0.tar.gz user@server:~/

# 在云主机上加载
gunzip -c mysql-8.0.tar.gz | docker load
```

---

## 📚 相关文档

- [Docker网络问题排查.md](./Docker网络问题排查.md) - Docker 网络问题排查
- [部署故障处理指南.md](./部署故障处理指南.md) - 部署故障处理
- [fix-deploy-issue.sh](./fix-deploy-issue.sh) - 部署问题修复脚本
- [fix-network-dns.sh](./fix-network-dns.sh) - 网络 DNS 修复脚本

---

## ✅ 检查清单

修复前检查：
- [ ] 网络连接是否正常（ping 8.8.8.8）
- [ ] DNS 解析是否正常（nslookup docker.io）
- [ ] HTTPS 连接是否正常（curl https://www.baidu.com）
- [ ] 云主机安全组是否允许出站流量
- [ ] 是否需要配置代理

修复后检查：
- [ ] DNS 配置是否正确（cat /etc/resolv.conf）
- [ ] Docker 镜像加速器是否配置（cat /etc/docker/daemon.json）
- [ ] Docker 服务是否运行（docker info）
- [ ] 能否拉取镜像（docker pull busybox）

---

**记住：云主机网络问题通常是 DNS 配置或安全组规则问题！先运行修复脚本，再检查安全组设置！**

